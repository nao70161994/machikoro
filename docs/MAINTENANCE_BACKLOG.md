# Maintenance Backlog

Last updated: 2026-08-04

This backlog is a maintenance inventory after the June 2026 safety/refactor cycles. It is not a request to continue broad refactoring. Use it to decide whether a future change is a small safe fix, a design task, a real-device verification task, or something that should be left alone.

For larger root-cause refactors, use `docs/ARCHITECTURE_REFACTOR_PLAN.md`. It defines the intended module boundaries, migration order, contract tests, rollback paths, and manual-verification gates before any broad split is attempted.

## Recently Fixed / Guarded

These risks are now covered by code changes, contract tests, or operations guidance. Do not remove these guards without replacing the same contract coverage.

| Area | Resolved risk | Current guard |
| --- | --- | --- |
| Socket payloads | Normal Socket.IO payloads could grow without a shared size/depth/string limit. | `SOCKET_PAYLOAD_LIMITS`, the injected `server/socketPayload.js` rejection gateway, and server tests for normal-vs-restore payload limits. |
| Restore audit | `undoBuild` live action payload and restore audit payload could be signed over different shapes, while sign/verify orchestration remained embedded in `server.js`. | `server/restoreAuditPayload.js` owns injected, pure snapshot/action payload shaping and canonical action data; `server/restoreAuditGateway.js` owns the injected snapshot/action sign/verify call order, exact action-log source, and boolean verification projection. Direct and server tests compare live `undoBuild` and restore canonical data and fix the gateway call sequence. Cryptographic bytes, dynamic key selection, restore authority, and protocol remain unchanged. |
| Restore audit runtime configuration | Active key/secret lookup and signing/verification option shaping were embedded in `server.js`, coupling environment policy to restore orchestration. | `server/restoreAuditRuntime.js` owns the injected dynamic config boundary and supplies options to `server/restoreAuditGateway.js`. Direct tests preserve per-call config reads, active key/secret selection, optional action-log source, verification keyring/freshness options, and fail-fast dependencies; signing bytes, verification results, restore authority, and protocol remain unchanged. |
| Compacted restore Snapshot attachment | Snapshot compression eligibility, residual-log check, signing, and action mutation were coupled in `server.js`. | `server/restoreSnapshotAttachment.js` owns only the injected eligibility and ordered attachment boundary. Direct tests fix the 200/201 threshold, empty residual log, audit call arguments, identity-preserving attachment, and no mutation on audit failure; compaction, signing, Socket delivery, protocol, and restore authority remain with existing owners. |
| Restore admission planning | Restore payload shape/size/room checks, canonical-vs-client source selection, audit validation, snapshot trust precedence, and new-room game-start/RL setting preparation were ordered inline with Socket effects in `handleRecreateRoom`. | `server/restoreAdmission.js` owns the injected admission plan. Direct table tests preserve every early exit, exact error text/result, canonical read laziness, source → audit → snapshot-verification order, canonical verification bypass, game-start/RL rejection before player-setting normalization, player index → expected token → token hash → original-host → reconnect-hash-set → restored-player ordering, hostless room-exists priority, and hostless omission of canonical/audit/signature authority, plus existing-room started-room, host-authentication, signed-sanitation, replace/reject/rejoin, and token-recheck ordering. `server/existingRoomRejoin.js` provides a separate default-OFF executor for detach → identity → optional host reselection/persistence → touch → `rejoinData` → `playerRejoined`, with inline legacy fallback. `server.js` retains `appError` emission and all room mutation, Socket, mirror, persistence, signing, sanitation, and protocol behavior. |
| Restore replay admission | New-room action-log sanitation, signed-audit requirement, empty-log trust gates, and canonical-vs-fallback rank calculation were coupled to room mutation in `handleRecreateRoom`. | `server/restoreReplayAdmission.js` owns the injected pre-mutation plan. Direct tests preserve secret-read → sanitize → empty-state gates → rank order, canonical signed-audit bypass after dynamic secret lookup, fail-before-rank behavior, trusted empty-log acceptance, and canonical hostEpoch/actionSeq fallback rules. Signing, sanitation algorithms, authority selection, metadata mutation, mirror construction, Socket delivery, and persistence remain unchanged. |
| Reconnect versioning | `rejoinRoom` payloads did not consistently carry `clientVersion`, weakening stale-client diagnosis. | `buildOnlineRejoinPayload()`, storage fallback payload helper, online/integration/storage tests. |
| Stats reset | Stats reset was a one-tap destructive action. | `clearStats()` uses custom `showConfirm()` when available; stats tests cover cancel/accept. |
| UI rendering safety | Card detail/build button text and color-derived classes could regress to raw HTML/attribute insertion. | UI tests cover card and landmark effect/category escaping and unsafe color fallback. |
| Client-error privacy | ntfy client-error bodies could leak reconnect/session/token-like values. | `scrubClientErrorText()` redacts URL query and token/session forms; server tests cover query, key-value, and JSON-like stack values. |
| Stats saved numbers | Corrupt saved stats could render `NaN`, negative, or extreme percentages/bar widths. | stats load normalization and render tests for finite non-negative numbers. |
| Restore action log | Unknown restore actions could pass sanitize and fail later during replay/rank logic. | `sanitizeRestoreActionLogEntry()` rejects unknown actions after snapshot skip gate; server tests cover invalid vs skipped old entries. |
| Guardrail docs | Future AI edits could remove safety contracts because they were scattered. | `docs/OPERATIONS.md` Maintenance Contract Guardrails and `tests/main.test.js` docs assertion. |
| Room lifecycle helpers | Pure room lifecycle and socket-presence policy was embedded in `server.js`, making handler edits too broad. | `server/roomLifecycle.js` owns player-list, start-payload, client-version, reconnect-token, disconnect-candidate, active/current socket, connected-host, host index/epoch synchronization, and hostless-capability projection helpers; server and direct tests cover pure behavior. |
| Room Socket runtime | `hostChanged` emission, stale-socket eviction, room replacement detach, and host-presence map wiring were embedded in `server.js`. | `server/roomSocketRuntime.js` owns the injected Socket effects. Direct tests preserve the `hostChanged` name/payload, appError → leave → same-room identity clear order, same-socket no-op, all-player ID clearing, and current socket-map host check; rejoin/disconnect/recreate decisions and protocol remain with existing handlers. |
| Socket payload limits | Normal and restore payload size/depth/string checks were embedded in transport wiring. | `server/socketPayload.js` owns unchanged pure limit checks plus the injected rejection gateway; direct tests fix the `appError` event and existing rejection message while handlers retain event names and behavior. |
| Server game settings | CPU/player/card setting normalization and RL model validation were embedded in `server.js`. | `server/gameSettings.js` owns the injected pure policy, including the existing weak/normal/strong/RL/expert display labels; the existing allowlists and exported server API remain unchanged. |
| Server game runtime loader | VM source order and server mirror export bindings were embedded in `server.js`, so adding a runtime dependency could silently drift across loaders. | `server/gameRuntimeLoader.js` owns the frozen four-file load order and 17-symbol export contract behind injected path/fs/vm adapters. Direct tests cover exact order, effects, real-source exports, and invalid injection; `server.js` retains the existing `loadGameRuntime` API and runtime wiring. |
| Room input validation | Player-name sanitation, room ID validation, and collision-safe ID generation were embedded beside Socket.IO handlers. | `server/roomValidation.js` owns the unchanged input rules; direct tests fix unsafe IDs, length limits, alphabet, and collision retry behavior. |
| Static asset policy | Root-file allowlisting and build-hash injection were mixed with Express route wiring. | `server/staticAssets.js` owns environment/Git/time-fallback build-hash resolution, injected index/public-root response handlers, and the unchanged pure allowlist/string transforms; PWA, online, and release tests retain delivery behavior. |
| Static directory routes | Directory route definitions could drift from `index.html` local asset references. | `server/staticAssets.js` owns frozen root/directory allowlists; `tests/static-assets.test.js` verifies every allowlisted root exists and every local index asset resolves through an allowlisted route. |
| Accepted action history | Duplicate-action lookup, bounded ACK memory, reconnect ACK references, and restore-aware room sequence advancement were embedded in `server.js`. | `server/actionAcceptance.js` owns player-scoped keys, legacy/action-log fallback, the 100-entry bound, minimal reconnect refs, and injected sequence advancement. Direct tests preserve lazy restore-rank fallback and `gameStartPayload.actionSeq` synchronization. |
| Canonical action payloads | Canonical key filtering and client action ID normalization were embedded in `server.js`. | `server/actionPayload.js` owns the frozen per-action key table and unchanged normalization; cross-layer action tests require registry, validator, canonical payload, and replay action sets to match. |
| Server action validation gateway | Live action validation order and server-authoritative dice canonicalization were embedded in `server.js`, so an early-reject edit could consume randomness or bypass actor/phase gates. | `server/actionValidationGateway.js` owns the injected mirror → winner → actor → phase → authoritative dice → payload order. Direct tests preserve every early exit, prohibit dice/payload work before gates, and fix room-to-mirror Undo fallback; Socket event names, payload shape, rules, and authority are unchanged. |
| Reconnect identity | Token generation/hash/restore comparison and verified rejoin-player mutation were coupled to room handlers. | `server/reconnectIdentity.js` owns the injected identity policy plus token-gated existing/restored player activation. Direct tests fix mismatch non-mutation, legacy hash completion, restored-player shape, and socket-ID replacement; tokens and reconnect protocol are unchanged. |
| Restore log sanitation | Snapshot sequence filtering, unknown-action rejection, canonical payload shaping, and audit carry-forward were embedded in restore orchestration. | `server/restoreSanitization.js` owns the pure sanitize pipeline; restore ordering, authority, signing policy, and handler timing remain unchanged. |
| Canonical mirror metadata | Stable state hashing and mirror marker construction were embedded in `server.js`. | `server/canonicalMirrorMetadata.js` owns pure metadata generation with direct parity tests; durable storage and canonical transaction remain out of scope. |
| Canonical mirror runtime | Marker/hash synchronization, stale rebuild diagnostics, Undo capture/clear, and accepted-action adoption were embedded in `server.js`. | `server/canonicalMirrorRuntime.js` owns the injected mutable runtime adapter with frozen build/clear action contracts. Direct and server tests preserve mismatch timestamps/warnings, rebuild/adoption behavior, and failure non-adoption; Engine authority and wire behavior are unchanged. |
| Server dice payloads | Authoritative dice recognition and payload generation were coupled to action handlers. | `server/serverDice.js` owns the injected pure dice payload policy; random source, event names, and action shapes are unchanged. |
| Server lifecycle reporting | Lifecycle notification normalization and text formatting were mixed with HTTP, auth, dedupe, and delivery. | `server/gameLifecycleReporting.js` owns pure formatting, `server/gameLifecycleGateway.js` owns request decision order/status, and `server/reportDelivery.js` owns injected ntfy delivery options; `server.js` retains route/catch wiring. |
| Server reporting auth/throttle | Origin/token checks and duplicate/rate-window algorithms were duplicated or embedded beside HTTP handlers. | `server/clientErrorAuth.js` owns unchanged request authorization; `server/reportThrottle.js` owns injected Map cleanup, rate counting, strict duplicate-window semantics, and the admission factory that binds each report family to its limits, buckets, cache, and dedupe-key projection. Request status/order live in the client-error and lifecycle gateways, notification option binding lives in `server/reportDelivery.js`, and `server/reportingHttpRoutes.js` owns injected Express route/JSON-limit/catch-fallback registration. `server.js` retains gateway construction and app dependency wiring. |
| Server rejoin payload | Snapshot/log/ACK/audit and provisional hostless fields were assembled inside reconnect flow. | `server/rejoinPayload.js` owns the injected payload builder and negotiated Snapshot wire composition; direct tests lock default/override/provisional fields, schema selection, exact encoder input, and fail-closed rejection while event names and handler timing remain unchanged. |
| Server game-start payload | Schema gate, player order, versions, reconnect hashes, and hostless capabilities were assembled in `server.js`. | `server/gameStartPayload.js` owns the injected payload builder. Direct and server/online/release tests preserve every field, schema fallback, random ordering input, and start/emit timing. |
| Server game-start lifecycle | Room initialization, canonical-mirror reset, timestamp publication, and persistence order were embedded in `server.js`. | `server/gameStartLifecycle.js` owns the injected ordered executor. Direct and server tests fix pre-reset mutations, reset-before-timestamp, persistence arguments, and fail-fast dependency validation; readiness, Socket emit, logging, and payload shape remain in `server.js`. |
| Server game-start coordinator | Readiness, payload rejection, room activation, Socket emit, and start logging were ordered inside `server.js`. | `server/gameStartCoordinator.js` owns the injected coordinator and fail-fast dependencies. Direct and server tests preserve missing/started/not-ready exits and build → mark → `gameStart` emit → log order; event name and payload identity are unchanged. |
| Online storage facade | Room-scoped storage keys, legacy fallback reads, and restore bundle cleanup were hard to audit inside `online.js`. | `js/onlineStorage.js` preserves existing key names/formats and room-scoped fallback contracts, and owns the pure maximum-action-seq policy used by restore-room index entries; direct and online tests cover default and injected policies. |
| UI pure render helpers | Build/pending/detail/select/winner/dice-choice/dice-display HTML, turn-announcement view/timing, card-selection state, and top-level tab projection lived inside `ui.js` or `main.js`, increasing escape, selector, and state-mutation drift risk. | Focused UI helpers now provide pure HTML/view/state contracts. `js/uiTabView.js` additionally owns local/online/stats, create/join, and online/offline availability projection; `js/uiLogDisplay.js` also owns collapsed icon/ARIA projection; `js/uiGameStatusView.js` owns turn text, roll/skip state, dice projection, active-turn transition state, and input-nonmutating coin deltas; `js/uiDiceDisplay.js` owns exact dice-face/result HTML and opacity projection; `js/uiTurnAnnouncer.js` owns human/CPU text, display state, and the unchanged 1300ms/400ms timing policy; `js/uiPlayerDisplay.js` also owns coin-animation class/text/sound projection while `js/uiCardDetail.js` owns card-effect text fallback and landmark effect/emoji projection; `js/uiStatsView.js` owns stats bucket selection, escaped filter/ranking HTML, empty-state copy, and overview projection while `stats.js` retains storage, recording, events, and the single DOM write; `js/uiInputPolicy.js` owns online block-reason priority, local/online human-turn projection, and allowed-action visibility while `js/uiBuildMenu.js` owns card-filter state transitions and `ui.js` retains runtime reads, mutable filter state, and DOM effects; `main.js` retains DOM insertion and the 1-second removal timer. `ui.js`, `main.js`, and `appShell.js` retain DOM writes, timers, and calls; rolling still leaves the prior opacity unchanged. |
| UI runtime state snapshot | `ui.js` assembled flow diagnostics while mixing game reads, CPU reads, clocks, and DOM-dependent rendering. | `js/uiRuntimeSnapshot.js` owns the input-nonmutating projection and pending-action normalization for the existing diagnostic schema. `ui.js` retains live runtime/clock reads, DOM effects, and its public `buildRuntimeStateSnapshot()` API; direct and integration contracts preserve field names and values. |
| Cross-layer action/selector contracts | Action metadata and UI child selectors could drift silently. | Server tests compare action registry, validators, canonical payload keys, and replay flags; main static tests compare interactability registry selectors with rendered UI sources. |
| Socket E2E reliability | Long same-process online E2E runs could lose a client heartbeat under host load and report an untraceable event timeout. | `tests/helpers/socket-e2e.js` owns the 15-second event wait, listener cleanup, failure context, and a test-only 120-second Engine.IO heartbeat tolerance that is restored after each server closes. The four-human completion path fixes its seed and player order; production heartbeat, reconnect behavior, and protocol are unchanged. |
| Action UI registry boundary | Physical action targets and child selectors were embedded in watchdog orchestration. | `js/actionUiRegistry.js` owns a frozen read-only manifest; direct tests require every game action exactly once with matching phase and selector coverage. |
| Restore payload validation | Game-start validation, undo sanitation, and restored human-slot reconstruction were embedded in `server.js`. | `server/restoreValidation.js` owns injected pure validation; `server/restoreGateway.js` owns canonical-vs-client source selection and the pure existing-room replace/reject/rejoin policy; `server/restoreAdmission.js` owns the injected shape/limit/room/source/audit/snapshot-trust, game-start/RL/setting-normalization, identity admission plans, and the existing-room started/token/signed-sanitize/replace-or-rejoin admission plan; `server/existingRoomRejoin.js` owns the separately gated ordered rejoin effect executor with inline fallback; `server/restoreReplayAdmission.js` owns signed-sanitize/empty-state/rank planning; `server/restorePreparation.js` coordinates game-start → identity → replay → metadata → room build → mirror preparation before any activation or Socket effect; `server.js` retains error emission and all later mutation/effects; `server/restoredRoom.js` owns input-nonmutating host/sequence/hostless metadata, exact-order application to the existing game-start payload, new/replace/reject activation decisions, deterministic mutable room-shell assembly, ordered accepted-action reconstruction and mirror preparation, canonical mirror-result planning, exact-order application to the restored room, and redacted completion-log/result planning and its injected log/result executor from already-validated inputs. Its separate default-OFF activation and delivery executors prevalidate dependencies, fix detach/delete/install and persist/join/identity/rejoinData order, and retain inline legacy fallbacks. Malformed table, gateway, server, online, and release tests preserve formats, priority, and effect order. `server/hostlessRestoreDiagnostics.js` projects coordinator events to a frozen room-hash/aggregate-only log shape, excluding raw candidates and tokens. |
| Pending outbound resend policy | Room ownership, restore-log append, and resend eligibility were embedded beside reconnect timing. | `js/onlinePayload.js` owns the unchanged pure policy plus rejoin pending reconciliation (replay-log, legacy snapshot compaction, accepted-client-action, or unaccepted reason) and the unsigned-snapshot action-log preservation decision; direct tests fix legacy seq fallback, room mismatch, duplicate append, CPU-host, human-turn, and reconciliation precedence cases. A production-uninjected test-only gate selects the pure reconciliation plan only on exact legacy parity. |
| Action Contract source | Phase, actor authority, payload keys, replay/apply flags, and UI targets could drift across runtime owners. | `js/actionContract.js` is the 15-action machine-readable source projected into `GameManager`, server canonical payload keys, and the UI registry. It also records accepted canonical payload variants such as the current/legacy `resolveMover` forms without changing live payloads; `server/actionValidation.js` also owns the fail-closed shuffled-player/CPU-host actor mapping; focused tests fix human and CPU authority. Report/tests still compare independent server validators and client apply behavior. |
| Scoped ESLint gate | Extracted pure helpers lacked a shared bug-finding lint gate. | ESLint 10.7.0 checks 199 browser/server maintenance files for undefined names, unreachable code, duplicate cases/imports, and constant conditions through `test:static`; a contract test prevents npm-script/config drift. Style and the five remaining side-effect client runtimes remain out of scope. |
| Batch final verification | Running overlapping npm group wrappers repeated the same tests and made maintenance batches unnecessarily slow. | `npm run test:batch` runs static checks, the complete unit/simulation set, the three standalone Socket E2Es, and release checks exactly once. A contract test fixes the command and rejects nested overlapping group wrappers. |
| Client report formatting | Error normalization, redaction inputs, report keys, and duplicate-window state transitions were embedded in `appShell.js`. | `js/clientReporting.js` owns pure URL query/hash removal, runtime-context projection, browser error/rejection/console input shaping, report formatting, and duplicate admission/next-state projection; `js/clientReportingTransport.js` owns the injected fetch-availability gate and unchanged POST/checkpoint sequence. Browser capture, clock, mutable dedupe state, global fetch binding, and watchdog effects stay in `appShell.js`. Direct boundary tests fix same-key suppression, exact-window acceptance, different-key acceptance, and frozen accepted state. |
| Client checkpoint recording | Snapshot fallback, in-memory trace bounds, and diagnostic persistence were embedded beside browser globals in `appShell.js`. | `js/clientCheckpoint.js` owns the injected record sequence, preserving the existing fallback shape, 80-entry memory limit, 5,000-character persistence limit, and exception absorption. `appShell.js` retains the DOM snapshot builder, real clock, browser root, existing `machikoroLastClientCheckpoint` key, and storage facade binding. |
| Client storage boundary | Diagnostic, lifecycle, freeze, PWA dismissal, local save/settings, reconnect, UI trace/tutorial, and statistics access repeated direct `localStorage` handling. | `js/clientStorage.js` is the sole direct browser-storage owner and provides injected get/set/remove/access/prefix enumeration. `js/appShellStorage.js` preserves its compatibility result, while `appShell.js`, `main.js`, `online.js`, `onlineStorage.js`, `storage.js`, `ui.js`, and `stats.js` delegate through the facade. Existing keys, JSON values, public wrappers, script order, reconnect/PWA behavior, and save formats are unchanged. |
| Lifecycle notification formatting | Opt-out parsing, notification-setting, runtime player/CPU counts, mode/version, immutable session/start/finish/reset transitions, finish-metadata, and winner CPU-difficulty projection, payload shaping, start signatures, session-ID formatting, reload dedupe parsing, marker serialization, and POST/checkpoint sequencing were embedded beside browser clock/RNG, storage, and fetch side effects. | `js/lifecycleNotify.js` owns the unchanged pure opt-out/notification-state/runtime-metadata/session-transition/finish-metadata/winner-CPU/payload/start contract and deterministic session-ID formatting from injected clock/RNG values; `js/lifecycleTransport.js` owns the injected POST/checkpoint sequence; `appShell.js` retains the actual clock/RNG calls, storage keys, global fetch binding, and notification timing. |
| Server client-error normalization | Notification payload normalization, classification, freeze summaries, UA/room presentation, and ntfy body formatting were coupled to server routes. | `server/clientErrorReporting.js` owns those pure transforms plus the injected dedupe-key field/600-character stack projection, and `server/reportingPolicy.js` owns trust-proxy/topic/rate-key/debug-endpoint/dedupe policy. `server/clientErrorGateway.js` owns request decision order/status and `server/reportDelivery.js` owns ntfy delivery option binding; HTTP route/catch wiring stays in `server.js`. |
| UI tutorial guidance | Phase/pending guidance text, affordable build candidate calculation, tutorial HTML formatting, and control display projection were embedded in DOM orchestration. | `js/uiTutorial.js` owns the unchanged pure decision/text/HTML and enabled/level control-view policy; `UiTutorialSettings.runtime.snapshot()` is the production read boundary for UI and settings persistence. Direct and wrapper tests cover every choice phase, pending kind, CPU/waiting state, build candidates, escaping, empty-tag output, and ON/OFF/level DOM projection. |
| CPU evaluation primitives | Pure dice frequency, score-cap, opponent-dilution, landmark-progress, combo, duplicate-purchase, economy-balance, lookahead gate, and special-card value calculations lived inside the giant CPU class. | `js/cpuEvaluation.js` owns the unchanged calculations, including station/card-color dice frequency, self-income by card effect, exact per-color/effect activation dispatch, dependency-injected purchase-value composition, expert lookahead gates, win-distance, opponent-threat, position-score composition, landmark shortfall/TV landmark-denial, expert roll-income cap/overflow penalty, strong conditional-red card/landmark pressure, strong color-role pressure, duplicate-renovation landmark-exposure risk, four-player expert card-candidate adjustment, strong dice-tempo/landmark-card synergy, strong purple adjustment/readiness, base/profile/strong landmark urgency, expert landmark-effect bonus, strong crowd purchase scoring, strong crowd attack scaling/disruption readiness, publisher/IT-startup/conditional-red/loan and card-dependency values, plus generic weighted outcome aggregation, stable score-descending card ranking with input-order ties, own-turn roll-income aggregation with the existing candidate/dormancy rule, weighted dice expected scores, Harbor alternatives, and the unchanged normal-difficulty safety adjustment; direct contracts, wrapper parity, decision snapshots, and full self-play regression guard behavior. |
| CPU player-count profiles | Pure option fallback and player-count profile selection lived inside the giant CPU class. | `js/cpuProfile.js` owns the unchanged constants and profile-name mapping; a fixed seed/action trace locks winner, turns, and the initial action sequence. |
| CPU live option policy | Live `expert` v2simple defaults were duplicated in `main.js` beside player construction. | `js/cpuTuning.js` owns both the input-nonmutating live-option projection and the complete constructor runtime-config projection, including exact preset/mode/finite-number/profile defaults and detached mutable maps. `CPU.js` retains typed field assignment and runtime strategy state; `main.js` retains CPU/RL construction. Direct config contracts, existing CPU tests, exact decisions, and 2–10-player self-play baselines prove unchanged strength. |
| CPU legal moves | Affordable landmark/card enumeration was duplicated inside expert and strong build selection. | `js/cpuLegalMoves.js` owns the unchanged pure filters, remaining enabled-landmark ordering/endgame threshold, plus stable player-target and Cleaning-card ranking/pruning for disruption choices; direct order contracts and decision/self-play baselines guard candidate ordering. |
| CPU Business Center moves | Minor-card enumeration, full/capped exchange candidate order, exchange-score composition, and random/simple selection were embedded in `CPU.js`. | `js/cpuBusinessMoves.js` owns those policies. Tests fix stable ties, early stop, the exact three random-choice calls, the legacy first-card double evaluation, and wrapper parity; 36 decision and 36 full-match baselines confirm no strength change. |
| CPU lookahead simulation | Deterministic playout RNG, weighted dice-outcome table, shop-stock construction, loop, and simulation-only phase steps lived beside game cloning and live CPU execution. | `js/cpuSimulation.js` owns the unchanged table, seeded LCG, injected 2–10-player stock mapping, and lookahead loop/step; CPU wrappers retain cloning and live execution ownership. |
| CPU action proposal | CPU decisions could pass mutable or non-canonical payloads directly into local/online execution. | `js/cpuActionProposal.js` validates every Action Contract payload variant, then returns a detached deeply frozen `{action, data}` proposal. All local rule-based CPU actions, including builds, apply through the shared mutable `GameEngine`; online build proposals continue through the existing authority and send path. `CPU.chooseBuildAction()` returns the proposal without mutating game/stock. Heuristic values, candidate order, RNG, and online authority remain unchanged. |
| CPU pending fallback ownership | Live scheduling retained a second copy of TV/Business/Mover/Renovation fallback selection beside the extracted CPU helper, and pending decisions exposed executable closures instead of the shared action boundary. | `js/cpuPendingResolution.js` now owns the shared fallback and Cleaning target validation/selection contract. Live pending decisions return detached, deeply frozen canonical `{action, data}` proposals without an `apply` closure; `main.js` retains scheduling and delegates effects through the existing shared action path, with a canonical-only compatibility executor for stripped test/legacy runtimes. The resolution API remains available to simulation callers. Main/CPU contracts, exact decision snapshots, and 2–10-player self-play baselines guard pending order, dormant-card filtering, candidate order, RNG, and strength. |
| CPU maintenance benchmark | Small traces did not cover all phases, player counts, or difficulty levels before scoring extraction. | 9 representative decision fixtures produce 36 exact snapshots; a second baseline fixes 36 full matches for all difficulties and 2–10 players. Both artifacts record a full source commit. |
| UI player/log/card ordering | Pure display normalization, full player-panel HTML, structured-log parsing/HTML formatting, and card ordering were embedded in `ui.js`. | `js/uiPlayerDisplay.js`, `js/uiLogDisplay.js`, and `js/uiCardOrder.js` own exact-output helpers; player-setting fallback diagnostics, history accumulation, DOM mutation, and event handling stay in `ui.js`. |
| UI log history state | Turn reset, reroll continuation, separator insertion, and the 300-entry bound were mutable inside `ui.js`. | `js/uiLogDisplay.js` now owns an input-nonmutating history reducer as well as HTML/view helpers. `ui.js` retains DOM writes, scroll effects, and the compatibility globals. |
| UI build action state | Build phase, pending, built-this-turn, human-turn, and allowed-action checks were assembled beside DOM writes. | `js/uiBuildMenu.js` owns the pure build gate/action-state projection, Undo visibility/enabled projection, and existing HTML helpers. `ui.js` captures runtime facts and retains DOM mutation; phase, online-input, selector, and display behavior are unchanged. |
| UI pending display gate | Pending phase/IT/renovation and human-turn checks were assembled beside DOM updates. | `js/uiPendingMenu.js` owns the pure display-candidate/current-player gate and modal/content/inner-style projection. `ui.js` preserves the legacy short-circuit order and retains DOM lookup/write, re-entry, and modal-policy effects. |
| UI watchdog diagnostics | Freeze-state classification and storage-size compaction were mixed with DOM snapshots, timers, recovery, and notifications. | `js/uiWatchdog.js` owns state-key/pending/classification and the pure freeze-facts projection from captured observations, trace compaction/root-cause mapping, element usability/lock reasons, primary/pending action classification, game-screen/post-build recovery eligibility, stale modal classification, pure 7KB diagnostic serialization, freeze-summary JSON formatting, recent-checkpoint trace compaction, stable sorted issue-dedupe signatures, ordered interactability-issue assembly from captured container/modal observations, freeze-classification eligibility/facts assembly, action-child requirement decisions, the seven recoverable freeze-kind allowlist and fail-closed handler selection, and render-recovery eligibility/target planning plus render-synchronization eligibility/human-lock issue selection from captured observations. `appShell.js` retains recovery policy/orchestration, focus-state coordination, storage writes, timing, reporting, and PWA behavior; `js/uiDomSnapshot.js` owns DOM observation and `js/uiRecoveryEffects.js` owns injected lock/display/class mutation primitives. |
| UI watchdog monitor | Freeze timing and duplicate-report suppression were mutable globals in `appShell.js`. | `js/uiWatchdogMonitor.js` owns reset, progress timing, the exact 5-second classification boundary, and 60-second report-vs-recover decision. `appShell.js` still captures DOM state and performs recovery, persistence, and reporting; duplicate reports remain suppressed without suppressing recovery. |
| Client runtime snapshot | Watchdog diagnostic schema assembly was mixed with browser-global and DOM reads in `appShell.js`. | `js/clientRuntimeSnapshot.js` maps grouped game, CPU, online, and DOM facts into the existing flat diagnostic shape. `appShell.js` retains all browser capture, recovery, persistence, timing, and reporting effects. |
| Crash screen policy | Error text shaping, saved-game resume visibility, initial focus, and focus-loop decisions were embedded beside DOM and CPU-stop effects in `appShell.js`. | `js/crashScreen.js` owns the unchanged 300-character message, resume/reload view, and pure focus-trap plan. `appShell.js` retains CPU cancellation, DOM mutation, listener binding, storage access, focus effects, and public wrappers; script order and SW cache inclusion are contract-tested. |
| Online payload boundary | Saved reconnect-session normalization, rejoin fields, restore action-log normalization, pending action normalization, and ACK matching were embedded inside storage/reconnect orchestration. | `js/onlinePayload.js` owns the unchanged pure validation, shapes, comparisons, and restore-event flush plan (generation/snapshot-seq filtering with original indexes). An independent legacy projection remains the default; the test-only `MACHIKORO_ONLINE_RECONNECT_QUEUE_PLAN_AUTHORITY_ENABLED` gate selects the pure plan only on exact parity and otherwise falls back; `storage.js` retains UI/resume coordination through the shared storage facade and `js/onlineRestoreQueue.js` owns the injected ordered executor and failed original-index result. A second test-only, production-uninjected queue-effect gate selects it only when the pure plan is authoritative; the independent legacy executor remains default. `js/onlineRestoreQueueState.js` additionally owns input-nonmutating enqueue/overflow, duplicate-rejoin generation carry, flush-start drain, failed-index suffix retention, and shared clear transitions behind an independent production-uninjected exact-parity gate. All disconnect/reset/game-start clear calls use that boundary and raw queue reads, replacements, and legacy appends are limited to three owner helpers. A detached store shadows each write. Independent production-uninjected read/write flags select store reads or store-first replacement/append only on exact legacy parity, with same-operation fallback to the raw mirror; `online.js` retains production queue mirror/write ownership and abort/handler execution, failure recovery, retry, callback, and socket ownership. |
| Online action sequence | Live sequence maxima and next-sequence arithmetic were embedded among storage reads and mutable reconnect state in `online.js`. | `js/onlineActionSequence.js` owns pure log maximum, current/last-applied projection, and one-step advancement. `online.js` retains storage reads, mutable applied-sequence memory, game-start patch writes, Socket timing, and protocol shape. Direct tests fix invalid-entry exclusion and the deliberate exclusion of game-start metadata from last-applied state. |
| Online restore rank | Restore authority ordering was embedded in reconnect orchestration. | `js/onlineRestoreRank.js` owns the unchanged epoch/replay-progress comparison; ACK, retry, queue, and callback timing stay in `online.js`. |
| Rejoin persistence effects | Runtime/session/restore-bundle writes before replay were one inline callback in `online.js`. | `js/onlineRejoinPersistence.js` owns an input-nonmutating plan and the exact action-flight/pending/retry/settings/indices/host/restore-bundle/session/CPU-token/UI-lock effect order. Separate production-uninjected plan/effect gates select it only on exact legacy parity; default and every mismatch use the inline legacy fallback. Storage keys, values, Socket events, replay timing, and production behavior are unchanged. |
| Post-restore pending resend | The accepted/stale/resend decision and ACK-flight/Socket emit order were embedded after restore activation. | `js/onlinePendingResend.js` owns the pure `none`/`clear`/`resend` plan and fixed clear-or-flight-then-emit executor. Separate production-uninjected plan/effect gates require exact decision and pending-reference parity; inline legacy remains default/fallback. Tests exercise both resend and stale-host clear through `rejoinData` without changing the `gameAction` event or payload. |
| Restore replay body | Game initialization, Snapshot hydration, residual Action replay, provisional log, and replay-mode cleanup were one inline try/finally block. | `js/onlineRestoreReplay.js` captures exact input references and owns the ordered replay executor, including unconditional replay-mode exit on failure. Separate production-uninjected plan/effect gates require full reference parity; inline legacy remains default/fallback. Screen switching, error status, and abort/retry remain in `online.js`; activation and queue flush now use their own boundary below. |
| Restore activation | Restored runtime activation, applied-sequence publication, queued-event flush, and activated lifecycle notification had order-sensitive inline effects. | `js/onlineRestoreActivation.js` owns the exact activation plan and ordered executor. It validates every handler before effects, stops before lifecycle publication when queue flush fails, and uses separate production-uninjected plan/effect gates with inline legacy fallback. Socket event names, payloads, queue ownership, and reconnect timing are unchanged. |
| Reconnect state observation | Reconnect phases had no shared names, event vocabulary, or bounded transition history. | `js/onlineReconnectState.js` freezes eight states, allowed transitions, lifecycle events, a pure event reducer, and event-vs-legacy projection comparison. `ONLINE_RECONNECT_EVENT_AUTHORITY_ENABLED=1` lets clean event history drive UI/send/CPU/human input gates. Independent test-only gates can then select the `isReconnectingOnline` compatibility boolean, rejoin timer handle/deadline, timeout ignore/rejoin/exhaust decision, ACK-timeout ignore/clear-only/rejoin plan, incoming gameAction and pending-matched actionAccepted no-game/duplicate/gap/apply plans, rejoin request reject/wait/exhaust/emit plan, terminal app-error cleanup decision, and the exact socket-disconnected, restore-started, replay-started, restore-activated, and normal retry-exhausted status messages from the same clean history; each stage falls back to its legacy owner on malformed, mismatched, invalid, or unsupported input. Start/disconnect/rejoin/restore, eight-attempt failure, status fallback, and projection fallback integration parity are fixed. Effect/status/timer/callback/request-plan/request-effect/queue-plan/queue-effect/queue-state/cleanup-effect/restore-abort-plan/action-timeout-plan/action-timeout-effect/game-action-plan/action-accepted-plan gates are not injected into production HTML. `js/onlineReconnectRequest.js` owns the fixed clear/count/emit/arm executor and centralizes the single `rejoinRoom` emit; it is selected only after pure request-plan authority plus its independent gate. `js/onlineReconnectCleanup.js` owns the fixed six-step terminal cleanup executor; it is selected only when the independent cleanup-effect gate and clean, legacy-matched cleanup decision both agree, otherwise the inline legacy sequence runs. `js/onlineActionTimeout.js` owns the fixed flight-clear/reconnect/CPU-token/status/rejoin order and stops after flight clear for clear-only plans; its independent effect gate requires an authoritative ACK-timeout plan, while inline legacy remains default. `js/onlineDecodeFailure.js` owns malformed Action wire recovery in the existing optional-flight-clear/reconnect/rejoin/retry order; separate gameAction/actionAccepted effect gates select it only with clean reconnect shadow state, and otherwise retain the inline legacy sequence. `js/onlineActionApplyFailure.js` owns error-report/reconnect/CPU-token/rejoin/retry order after a live Action apply exception and suppresses immediate rejoin during restore queue flush; its separate handler gates require an authoritative pure apply plan, with inline legacy fallback. `js/onlineActionGap.js` owns reconnect/CPU-token/optional-status/rejoin/retry ordering for sequence gaps, while `js/onlineActionNoGame.js` preserves incoming rejoin versus accepted status-only behavior; both use separate handler gates only after their authoritative pure plans and otherwise fall back inline. `js/onlineActionCommit.js` owns the successful set-sequence/save-log/accepted-only pending-clear/render/CPU-schedule order; restore queue flush omits render/schedule, and separate handler gates require authoritative pure APPLY plans before selecting it, with inline legacy fallback. `js/onlineSocketConnect.js` owns waiting-status cleanup and rejoin eligibility plus their ordered effects, selecting them only from a clean CONNECTING history. `js/onlineSocketDisconnect.js` owns active/restore-abort planning and the lobby-finish/optional restore quarantine/reconnect/flight/CPU/event/status order. Both callback families have separate production-uninjected plan/effect gates and inline legacy fallback. `js/onlineHostChanged.js` owns pure host ownership planning and the host-state/log/render/CPU-schedule-or-invalidate/persistence order after restore queue admission; separate production-uninjected plan/effect gates preserve inline legacy fallback. `js/onlineRestoreAbort.js` owns the fixed finish/quarantine/queue/reconnect/status/rejoin/retry effect order; its independent effect gate requires an authoritative pure abort plan, and the inline legacy sequence remains the default. Production Socket emit authority, session read, hostless branching, context-specific status text beyond socket disconnect/restore lifecycle/retry exhaustion, restore queue ownership/abort/handler-flush authority, production restore-abort state/UI/rejoin effect authority, storage format, protocol, and production cleanup authority remain legacy. The restore-abort gate only selects a generation/status/queue-reference plan after clean event history and exact legacy parity; queue overflow is covered through disconnect/connect/rejoin/restore automation. |
| Online runtime flag registry | 53 schema/reconnect/engine rollout flags were read through repeated direct `window.MACHIKORO_*` checks and then through 49 hand-written compatibility wrappers in `online.js`, making typo, truthiness, and newly-added reader drift hard to review. | `js/onlineRuntimeFlags.js` is the frozen name-to-window-property source, accepts only strict boolean `true`, and creates frozen named readers with call-time root resolution and unknown-name rejection. `online.js` preserves all public reader names through one destructured adapter; the four schema-transport wrappers retain their prerequisite composition. Production injects none of the authority flags, so every existing default-OFF/fallback path is unchanged. |
| Online schema transport adapter | Version/flag negotiation and Action/Snapshot/recreate codec selection were interleaved with socket orchestration in `online.js`. | `js/onlineSchemaTransport.js` owns the injected negotiation prerequisites, capability/selection checks, and codec dispatch. `online.js` keeps its public wrapper names and live negotiated selection; all production flags remain absent/default-OFF, and legacy payloads plus existing error reasons are unchanged. |
| Client/server replay parity | Client apply and server mirror could evolve to different final snapshots despite per-action coverage. | An online contract test applies the same representative action trace to both paths and compares complete serialized snapshots. |
| Card-select state/view | Selection mutation, set ON/OFF projection, sorted toggle HTML, and attribute escaping were embedded in modal orchestration. | `js/uiCardSelect.js` owns input-nonmutating card/landmark reducers and a frozen view model. Required cards, minimum-one landmark, ordering, set state, and exact HTML are direct contracts; event handling, modal state, and DOM writes remain in `ui.js`. |
| Card effect registration | Stable IDs, effect descriptions, rule handlers, and CPU references could drift across layers. | `tests/card-contract.test.js` checks definition identity, known/used effects, UI descriptions, income handlers, rule references, and CPU references. |
| Snapshot roundtrip fixtures | Roundtrip coverage did not explicitly name representative pending, undo, landmark, multiplayer, mixed-pending/dormancy, and endgame states. | `tests/snapshot-contract.test.js` and its fixture builder require exact serialize/restore/serialize equality across those states, then directly assert restored pending-action authority, mixed Cleaning→Renovation queue order, dormant-card indexes, dice state, 10-player current index, Undo retention, and post-victory action closure. |
| Shared snapshot serialization/hydration | Client, server mirror, local save, and local Undo could construct and restore equivalent state through separate implementations. | `js/gameSnapshot.js` owns exact snapshot/undo/local-save serialization plus shared mutable game/Undo hydrate cores. Local save generation, local Undo generation, local Undo restore, and server mirror Undo restore delegate to it; the local-save reader accepts both legacy state and Snapshot v1 envelopes through one fail-closed adapter. `js/localSaveRepository.js` keeps `savedGame` as the rollback authority and may prefer a validated v1 shadow only while its independent flag is enabled. Caller adapters preserve strict validation, legacy normalization, landmark/inventory policy, CPU recreation, UI effects, the exact legacy write shape, and existing full-log Undo retention. |
| Local settings normalization | Saved player-count, player-setting, tutorial normalization, and persistence-value projection were embedded beside storage and DOM effects. | `js/storageSettings.js` owns the unchanged pure normalization plus save/load value projection. `storage.js` retains existing keys, facade-backed storage access, DOM updates, control synchronization, and exception handling. Direct and storage integration tests preserve old defaults, malformed-number behavior, exact JSON/string values, and absent CPU-speed handling. |
| Local player settings view | Local name/default normalization, CPU labels, RL guidance, speed text, and player-setting HTML were embedded beside game start and preload effects in `main.js`. | `js/localPlayerSettings.js` owns the pure normalization/view contract, including RL readiness text and start-button state, with direct input-nonmutation, escaping, option, label, snapshot, pending-priority, and runtime load-order tests. `main.js` retains mutable UI state, DOM assignment, RL preload, persistence calls, and game start. |
| Local auto-skip availability | Card stock, coins, unique-purple ownership, and enabled-landmark purchase checks were embedded beside timeout and Action effects in `main.js`. | `js/autoSkipPolicy.js` owns an input-nonmutating frozen availability projection with candidate-order and exclusion contracts. `main.js` retains the unchanged eligibility gates, 1.5-second timeout, turn/phase revalidation, online authority, and `nextTurn` effect. |
| Page activation diagnostics | CPU scheduler resume classification and hidden-duration normalization were embedded beside visibility, timer, and checkpoint effects in `main.js`. | `js/pageActivationPolicy.js` owns both pure projections with direct tests for every outcome and invalid/clamped durations. Thin wrappers preserve the existing call sites; `main.js` retains `Date.now()`, visibility state, CPU rescheduling, token/timer effects, and checkpoint publication. |
| CPU scheduler state | Lease state, scheduling block priority, and phase eligibility were embedded beside timers and CPU execution in `main.js`. | `js/cpuSchedulerState.js` owns pure wait/deadline/token/health projection plus replay/host/reconnect/flight/socket/game/winner/human-turn block-reason priority and phase-step eligibility. `main.js` retains lazy winner evaluation, `Date.now()`, mutable tokens, `setTimeout`, checkpoints, phase execution, and CPU action timing; main/integration and direct tests preserve stalled-turn diagnostics and scheduling behavior. |
| CPU non-build turn strategy | Dice selection, reroll, Harbor, pending, next-turn, and IT decisions were interleaved with timers and mutable action execution in `main.js`. | `js/cpuTurnStrategy.js` returns canonical frozen action proposals while preserving decision-before-RNG and exact RNG consumption order. `main.js` retains phase checks, checkpoints, timers, `cpuDo`, pending application, and all game effects; build execution remains in its existing boundary. Direct/main tests plus exact decision and 2–10-player self-play baselines guard behavior and CPU strength. |
| Delayed human action resume policy | Page activation mixed delayed-action eligibility, deadline classification, and timer effects in `main.js`. | `js/delayedHumanActionPolicy.js` owns the pure idle/cancel/run/reschedule decision with direct priority and deadline-boundary tests. `main.js` retains mutable state, action authority checks, clock reads, token invalidation, timer scheduling, and callbacks; dice selection and roll behavior are unchanged. |
| Local human action authority | Local/online turn ownership, stale delayed-player, CPU-turn, reconnect, ACK-flight, and socket-connect gates were ordered inline in `main.js`. | `js/localActionPolicy.js` owns the pure fail-closed reason priority and boolean projection. `main.js` retains game/winner evaluation, reconnect state reads, Socket/global state collection, Action Contract validation, timers, and all action effects. Production script/SW/test load order and direct/main contracts preserve existing local and online input behavior. |
| UI event delegation boundary | Delegated target lookup, data-attribute conversion, and keyboard-role checks were duplicated beside action dispatch in `main.js`. | `js/uiEventDelegation.js` owns the exact closest/fallback dataset, Enter/Space role-button, immutable family command decoding, and injected command execution contracts. `main.js` retains listener registration, preventDefault timing, lazy effect registries, DOM/PWA effects, and public handlers. |
| Title skyline renderer | Roughly 180 lines of title-screen Canvas drawing and randomness were embedded in game/start orchestration in `main.js`. | `js/citySkyline.js` owns the unchanged renderer behind an injected viewport/RNG boundary. Direct tests lock dimensions, Canvas command coverage, and RNG use; a mechanical source-parity check verified the moved body. `main.js` now only resolves the canvas and delegates, while the Service Worker asset and runtime load order remain synchronized. |
| Online player settings and lobby view | Online setting normalization, exact option HTML, RL model freezing/readiness messages, and create/join button decisions were embedded beside Socket.IO and timeout orchestration. | `js/onlinePlayerSettings.js` owns those pure contracts plus the unused/loader-missing/idle/portfolio RL load-state selection. `online.js` retains mutable settings, DOM assignment, RL preload, lobby timeout, Socket emit, and create/join request ordering. Existing payload fields, player order, labels, and timing are unchanged. |
| Local save validation boundary | Save validation, legacy CPU setting normalization, pending consistency, and card-ID stock compatibility were embedded in `storage.js` beside DOM, preload, and resume effects. | `js/savedGameValidation.js` owns the injected fail-closed validation and legacy normalization contract. `storage.js` retains storage keys, facade-backed JSON I/O, hydrate adapters, CPU recreation, UI effects, and reconnect timing; direct and integration tests preserve the unversioned save shape. |
| Local resume policy | Save existence/pending gating, RL preload selection, and saved CPU construction arguments were interleaved with storage, hydration, DOM, and scheduler effects. | `js/localResumePolicy.js` owns the pure ignore/no-save/invalid/preload/resume decision, unchanged live CPU construction plan, detached runtime settings, and the exact invalidate→cancel→reset→settings→hydrate→CPU→presentation→screen→render→schedule effect order. `js/localResumeView.js` additionally owns the RL-preload button and local/online resume-section projection. `storage.js` retains repository I/O, exact save schema/key, RL Promise handling, actual hydration, DOM, and scheduler callbacks. |
| Stored online reconnect effects | Applying a validated reconnect session, initializing Socket.IO, rolling back a failed socket start, and sending rejoin were ordered inline in `storage.js`. | `js/storedOnlineReconnect.js` owns the normalized runtime plan and exact effect order. `storage.js` retains the existing session key/format, DOM notice, exception cleanup, and actual Socket callbacks. |
| Local game start effects | RL loading decisions and local start effects were interleaved in `main.js`. | `js/localGameStart.js` owns pending/loading/preload decisions, detached settings, and the existing runtime→settings→stats→online→UI lock→screen→init→lifecycle order. `main.js` retains Promise handling, DOM access, real game initialization, CPU construction, and settings persistence. |
| Shared action execution | Client replay, server mirror, and local CPU orchestration maintained overlapping action application paths. | `js/gameEngine.js` owns the shared mutable dispatch table with injected card, stock, and undo adapters. Client replay, server mirror, and every local rule-based CPU action application delegate to it; client/server validation, actor authority, compatibility policy, timing, and online transport remain in their existing owners. |
| Pure transition shadow | A future immutable engine cutover had no detached, fail-closed comparison seam. | `GameEngine.transitionSnapshot()` clones inputs, hydrates an isolated runtime, applies one shared action, and returns a detached snapshot with stable failure reasons. Server mirror parity covers every Action Contract entry across all four independent Action/Snapshot v0/v1 selections and richer 2/3/5/10-player traces, including Airport no-build income, Airport-to-IT pending, Amusement Park doubles, and Shopping Mall red-then-green income ordering. `js/gameEngineAuthority.js` owns the shared fail-closed authority policy used by the server wrapper and `js/gameEngineClientShadow.js`. The server can adopt only after transition/parity/reconstruction success. Deterministic online replay has separate production-uninjected shadow/authority flags and adopts only after stable snapshot parity plus detached rebuild validation. `js/gameEngineRuntimeAdapter.js` shares hydrate/serialize/Undo compatibility across online and local callers, while `js/gameEngineDeterminism.js` excludes unresolved random inputs. Local human actions, CPU proposals, card/landmark builds, and Undo now use separate production-uninjected shadow/authority flags; exact parity and detached reconstruction are required before adoption. All 15 Action Contract entries now converge with legacy through the real local adapter, including dice/Harbor, every pending resolver, build, Undo, and turn transition; all failures retain the already-applied mutable result. Production remains OFF. |
| Versioned engine transition footing | Action/Snapshot envelopes had no composed engine boundary for a future negotiated rollout. | `GameEngine.transitionEnvelope()` preserves legacy no-selection behavior, while an optional negotiated selection enforces independent Action/Snapshot input versions and emits the selected Snapshot version. `js/gameSchemaCodec.js` owns fail-closed encode/decode. The engine boundary remains shadow-only. Default Socket.IO traffic and all saves remain unversioned; only the separately gated live Action adapter may emit negotiated v1. |
| Schema capability negotiation | Additive transport boundary implemented behind an off-by-default rollback flag | `GAME_SCHEMA_NEGOTIATION_ENABLED=1` injects a client opt-in, validates create/join/rejoin advertisements, treats missing old-client capability as legacy v0, and records the all-human common Action/Snapshot versions in `gameStart.gameSchema`. A real Socket.IO E2E covers v1 selection, legacy fallback, and reconnect rejection; the flag remains off by default. |
| Versioned live Action wire | Negotiated Action v1 needed a separately reversible transport step. | `GAME_SCHEMA_WIRE_ENABLED=1` has effect only with negotiation enabled, envelopes v1 live `gameAction`/`actionAccepted`, keeps legacy rooms unversioned, preserves transport metadata, and rejects version mismatch before validation/replay. It composes independently with the Snapshot wire flag; Socket.IO E2E covers v1, legacy fallback, malformed rejection, and rejoin. |
| Versioned Snapshot transport | Negotiated Snapshot v1 needed an independent rollback boundary. | `GAME_SCHEMA_SNAPSHOT_WIRE_ENABLED=1` requires negotiation and versions only `rejoinData.stateSnapshot` plus compacted `gameAction`/`actionAccepted` Snapshot metadata. It composes independently with Action wire v0/v1, fails closed before hydrate, and has real Socket.IO E2E for v1 and legacy fallback. Default OFF; local `savedGame` remains legacy authority. Recreate requests use their own independently gated adapter described below. |
| Versioned recreate request transport | `recreateRoom` had no schema boundary for its outer request, Snapshot, or residual action log. | `js/recreateRoomPayload.js` owns the outer legacy/v1 envelope and `js/gameSchemaRecreateWire.js` composes negotiated Snapshot/action transforms while preserving seq/client/audit metadata. `GAME_SCHEMA_RECREATE_WIRE_ENABLED=1` requires negotiation, accepts unwrapped legacy without nested conversion, rejects unknown/malformed outer or nested versions before restore effects, and is covered by VM plus real Socket.IO E2E. The server decodes to the exact legacy form before existing signature, sanitation, and authority logic. Default OFF; restore authority and stored formats are unchanged. |
| Local-save v1 shadow rollout | Directly replacing `savedGame` would break old-version rollback and could resurrect a deleted save. | `LOCAL_SAVE_SCHEMA_WRITE_ENABLED=1` dual-writes a v1 envelope to `savedGameV1` while always preserving the exact legacy `savedGame`. The repository adopts only a validated v1 shadow when legacy still exists, falls back on malformed/failed writes, and deletes both through the new client. Default OFF; production activation is not performed. |
| Schema action shadow | Negotiated versions had no evidence from the real accepted-action path before live cutover. | `GAME_SCHEMA_SHADOW_ENABLED=1` works only with negotiation enabled and compare-runs accepted server actions through the shared mirror adapter. Match/mismatch/error remains diagnostic-only and never blocks acceptance, ACK, broadcast, or persistence. The default remains off; Socket.IO E2E fixes a matched v1 action and detached server parity covers all 15 actions for 2/3/5/10-player boundaries under 0/0, 0/1, 1/0, and 1/1 Action/Snapshot selections. |
| Runtime module dependency order | Extracted browser-global modules could be omitted from a VM/self-play loader while production still worked. | `tests/runtime-dependencies.test.js` fixes dependency-before-consumer order for production and the principal test/self-play runtimes. |
| PWA install controller | Install-prompt state, banner priority, dismissal storage, and browser event binding were embedded in `appShell.js`. | `js/pwaShell.js` owns an injected controller; direct, main, release, static-asset, and SW tests preserve update-banner priority, standalone behavior, script order, cache inclusion, and the existing public wrapper names without changing the SW update flow. |
| Canonical store authority contract | A storage adapter could be called durable without retention, CAS, or locking guarantees. | `server/canonicalStateStore.js` validates six methods and four explicit capabilities; `server/canonicalStateRepository.js` owns injected record construction, save/load delegation, room/schema validation, and failure isolation. Noop remains default and memory remains non-authoritative. |
| Restore signing rotation | A single restore secret had no key ID, overlap, or freshness contract. | `server/restoreAuditKeyring.js` supports active/old keys, bounded parsing, max age, and skew while preserving legacy single-secret behavior. |
| Restore authority priority | Durable, signed, host replay, and quorum candidates lacked one fail-closed priority model. | `server/restoreAuthorityPolicy.js` fixes the intended order and rejects conflict/invalid/completed higher authority; it is a tested footing, not an enabled production dispatcher. |
| Socket handler families | Lobby, rejoin, accepted-action, and disconnect sequencing were embedded directly in the server entrypoint. | `server/lobbySocketHandlers.js`, `server/rejoinSocketHandler.js`, `server/actionSocketHandler.js`, and `server/disconnectSocketHandler.js` use dependency injection; tests lock registration, hostless-before-room disconnect order, stale-socket rejection, host transfer, persistence, and effect/emit order while event names and default payloads stay unchanged. |
| Online retry policy | Retry constants, exhaustion boundary, deadline, and status text were embedded beside socket callbacks. | `js/onlineRetryPolicy.js` owns the unchanged 3s/8-attempt/15s constants, a timer handle/deadline controller, and the pure ignore/rejoin/exhaust timeout decision. Test-only gated E2E covers controller authority and legacy fallback; callback side effects and ordering remain in `online.js`. |
| CPU build execution | Local/online card and landmark side effects were embedded beside selection. | `CPU.chooseBuildAction()` captures one canonical proposal from the unchanged selection branches without mutating game/stock; `js/cpuBuildExecution.js` resolves and applies that proposal exactly once to local or online execution. Exact decision and 2–10 player/all-difficulty baselines confirm unchanged strength and RNG behavior. |
| Modal lifecycle policy | Blocking modal registry, visibility decisions, nesting, focus-trap decisions, and accessibility-sensitive open effects were coupled to DOM effects. | `js/uiModalPolicy.js` owns the deny-by-default policy plus pure visibility/focus-trap and Escape/Tab keydown command decisions. `js/uiModalOpen.js` owns the exact modal identity plan and capture-focus→active owner→body class→visual normalization→dialog attributes→focus→inert executor. `js/uiModalClose.js` owns the post-hide active-owner/unlock/pending-render/focus-restore/trace plan and ordered executor. Each production-uninjected flag selects its executor only after exact legacy-plan parity; inline legacy remains default/fallback. Modal hide, active-owner policy input, focus trap, inert/pointer handlers, and events remain in `ui.js`. |

## Backlog Classification

Severity means maintenance risk, not necessarily user-facing bug severity.

### Critical

No current Critical maintenance item is known from this review. The remaining trust-boundary items below are important, but they are explicitly design-scoped rather than urgent small fixes.

### High

| Item | Classification | Risk | Impact | Suggested action | Deferred reason |
| --- | --- | --- | --- | --- | --- |
| Host-supplied restart restore trust boundary | Staged footing complete; production choice required | Adapter capabilities, signing rotation/freshness, and fail-closed priority are testable, but default storage is noop and live dispatcher still uses the compatible host/provisional path. | Competitive/public trust would still be overstated without a real durable provider and operations policy. | Select backend/retention/locking and secret rotation, run migration/rollback gates, then separately review activation. | Provider, production state, retention/privacy, and secret operations require human/operator judgment. |
| Provisional hostless restore | Implemented; real-device timing verification remains | The fail-closed quorum path can recover an absent-host room only after host grace, exact agreement, and explicit confirmation. | Availability improves without allowing one-client or majority selection, while client-carried state remains lower trust. | Keep restored-room replacement host-only and preserve `HOSTLESS_RESTORE_ENABLED=0` as the immediate rollback. | Full 60s grace + 30s collection + confirmation rotation is not yet verified on mixed real devices. |
| Long-running real online play and reconnect | Partially verified on real devices | A four-player mixed-device match (Android ×2, iPhone ×2) completed through victory with reconnect on 2026-07-18. | Basic mixed-device synchronization and reconnect continuation have direct evidence; specialized recovery paths remain. | Keep automated gates and manually verify host migration, server restart restore, Undo around reconnect, online CPU, background/resume, and PWA update paths separately. | One completed match does not cover every reconnect/PWA/modal failure mode. |

### Medium

| Item | Classification | Risk | Impact | Suggested action | Deferred reason |
| --- | --- | --- | --- | --- | --- |
| `server.js` restore and HTTP orchestration remain large | Partially migrated; further movement needs design | Lobby create/join, rejoin, gameAction, and disconnect handlers now have dependency-injected families with exact order tests; restore validation/signing/sanitation, Socket effects, mirror/persistence sequencing, and HTTP wiring remain in `server.js`; pure restore source and existing-room decision policy now live in `server/restoreGateway.js`; restore entrance checks, source/audit ordering, snapshot trust precedence, game-start/RL setting preparation, identity gate, and existing-room replace/reject/rejoin admission orchestration now live in `server/restoreAdmission.js`, while the default-OFF ordered existing-room rejoin executor lives in `server/existingRoomRejoin.js`, and signed-sanitize/empty-state/rank planning lives in `server/restoreReplayAdmission.js`; `server/restorePreparation.js` now owns their external-I/O-free preparation sequence through mirror readiness, with exact lazy-order tests; validated restore metadata planning/exact-order payload application/activation/mirror-result planning/exact-order room application/redacted-completion planning/log executor, room-shell assembly, accepted-action reconstruction/mirror preparation executor, and separate default-OFF detach/delete/install and persist/join/identity/rejoinData executors with inline legacy fallbacks live in `server/restoredRoom.js`; game-start payload assembly lives in `server/gameStartPayload.js`. | Remaining edits can still cross restore trust and Socket/mirror/persistence boundaries. | Move another family only with exact status/effect-order contracts and online/release E2E. | Moving the remaining validation/signing/mirror/log effects would change trust or diagnostic ordering; broad HTTP movement adds little value without a concrete route change. |
| `js/CPU.js` remains a giant mixed file | Now safer, further movement needs design judgment | Diagnostics, evaluation primitives including card/station dice frequency, per-effect activation dispatch, purchase-value composition, Cleaning value feature projection, win-distance, opponent-threat, position-score composition, landmark shortfall/TV landmark-denial, expert roll-income cap/overflow penalty, strong conditional-red pressure, strong color-role pressure, dice-tempo/landmark-card synergy, purple adjustment/readiness, landmark urgency, expert landmark-effect bonus, crowd purchase scoring, crowd attack scaling/disruption readiness, and lookahead gates, base legal-move filters, Business Center enumeration/scoring/random-simple selection, weighted dice outcomes, lookahead loop/steps, pending resolution, cache, tuning, and player-count profiles now have seams. All rule-based build strategies now select a canonical action-only proposal before `js/cpuBuildExecution.js` applies it, and non-build live turn decisions return canonical proposals through `js/cpuTurnStrategy.js` before `main.js` performs existing effects. Large scoring and candidate orchestration methods remain coupled. | Moving or rewriting scoring/tie-break orchestration can still alter candidate order, caches, profiling, or RNG consumption. | Extract another scoring unit only with direct wrapper parity plus exact decision and full self-play evidence. | Broad scoring/selection decomposition remains deferred until a concrete stable boundary can preserve order and RNG; the action-only build cutover itself is complete. |
| `js/ui.js` still owns modal/render orchestration | Now safer, still medium | Build/pending/detail/select/player/log/order/tutorial/stats view helpers, pure modal policy/state including keydown commands, default-OFF modal-open and post-hide modal-close plan/effect boundaries, and top-level tab view projection are split. `stats.js` retains only stats storage/recording/events/DOM insertion; modal hide/focus-trap/inert handlers, general DOM mutation, and event processing remain in `ui.js`. | Selector drift is better guarded, but modal/focus/pointer regressions still need dedicated real-device checks. | Keep modal lifecycle stable until an iPhone/Safari modal matrix is scheduled. | Mixed-device online completion did not exercise modal focus/inert/accessibility; large UI movement needs targeted visual/mobile checks. |
| `js/online.js` reconnect/session orchestration remains complex | Staged read/boolean/timer/timeout/request-plan decisions and request effect, terminal cleanup decision/effect, five status effects, queue plan/executor, and restore-abort and ACK-timeout plan/effect executor boundaries complete; broader side-effect authority remains | Storage, payload normalization/ACK matching, player-setting/lobby view decisions, pending ownership/resend policy, restore rank and newer-original-host bundle offer decision, event state, retry calculations, compatibility boolean, timer handle/deadline, timeout decision, ACK-timeout plan and ordered effect executor, incoming gameAction/actionAccepted plans, pending reconciliation reason/acceptance plan, unsigned-snapshot full-log preservation plan, original-host local-bundle offer plan, rejoin request plan/effect executor selection, terminal app-error cleanup decision/effect executor, socket-disconnected/restore lifecycle/normal retry-exhausted status selection, restore queue flush plan/executor selection, raw queue read/replacement/legacy-append owner helpers, parity-gated detached shadow-store read/write boundaries, and enqueue/overflow/generation-carry/drain/failure-suffix/clear pure transitions, and restore-abort generation/status/queue plan plus ordered effect executor, and ACK-timeout ignore/clear-only/rejoin plan plus ordered effect executor, and incoming gameAction and pending-matched actionAccepted no-game/duplicate/gap/apply plans, plus malformed Action decode recovery with its actionAccepted-only flight clear apply-failure recovery with restore-flush suppression, sequence-gap recovery, and no-game recovery, successful sequence/log/pending-clear/render/CPU-schedule commit effects, and socket connect/disconnect callback plans and ordered effects, and host ownership/CPU scheduling/persistence effects, plus pre-replay runtime/session/restore-bundle persistence effects, restore replay body effects, restored-runtime activation/sequence/queue-flush/lifecycle effects, and post-restore pending clear/resend effects, have gated or focused helpers. Activation and pending clear/resend executors remain default-OFF and require exact authoritative plan parity; inline legacy remains the production owner and fallback. UI/send/CPU/human input reads and test-only effect steps require clean parity and fail closed to legacy. Production Socket emit authority, session read, hostless branches, restore queue ownership/abort/handler-flush authority, production restore-abort/rejoin callback authority, production ACK-timeout authority, context-specific status beyond socket disconnect/restore lifecycle/retry exhaustion, and production effects remain in `online.js`. | Small callback/order mistakes can resurrect stale room data or drop pending actions. | Keep effect flags production-OFF; move one remaining side effect only with exact ordering, fallback, automated reconnect E2E, and a separate rollback flag. | Remaining writes alter live socket/UI timing and require broader device/operational evidence; automated parity is sufficient only for the completed staged boundaries. |
| Pure Game Engine live migration remains | Shared dispatch, hydration core, runtime adapters, determinism policy, shared authority policy, and server/online/local shadow-adoption boundaries complete; production cutover needs design | `js/gameEngine.js` owns shared action dispatch, `js/gameSnapshot.js` owns serialize/hydrate mechanics, and `js/gameEngineRuntimeAdapter.js` owns caller-compatible runtime reconstruction. The server canonical mirror, deterministic online replay, and resolved local human/CPU/build/Undo paths can each shadow and adopt only through independent default-OFF or production-uninjected gates with exact parity, detached reconstruction, and legacy fallback. `js/gameEngineDeterminism.js` keeps unresolved random local inputs outside the boundary. Four schema selections across 2/3/5/10 players cover the all-action and richer multiplayer traces, including Tuna Boat dice retained through the Station → reroll → Harbor choice chain and conditional-red landmark thresholds through Station selection, Harbor choice, full transfer, and City Hall recovery, plus two queued Mover resolutions that preserve dormant/active card identity and queue order, and a Cleaning → two-Renovation mixed queue that preserves all-player dormancy and automatic no-target consumption. | A premature production cutover could change stock/Undo adapters, callback order, online authority, CPU scheduling, or mixed-client behavior. | Keep multi-action shadow parity expanding with each newly touched rule path before enabling or widening a live authority gate. | Production activation of server, online-client, or local pure authority and adoption of unresolved-random transitions require separate rollout/rollback review. Local-save dual-read/dual-write shadow footing is complete, but production activation remains deferred. |
| Test files are also giant | Now acceptable | `tests/server.test.js`, `tests/cpu.test.js`, and `tests/online.test.js` are large. | Future test edits may be hard to localize. | Split only by stable domain boundaries when adding new tests becomes painful. | Mechanical test moves can create noise without behavior value. |
| JSDoc/checkJs/ESLint | Scoped typed boundaries and ESLint expanded | JSDoc contracts are enforced by TypeScript 5.9.3 in no-emit checkJs mode for an explicit allowlist of 222 browser/server runtime files, including `CPU.js`, `RLCPU.js`, `server.js`, and every extracted `server/*.js` module. `npm run test:types` runs through `test:static`; ESLint covers 223 maintenance files with bug-detection rules and config/script drift tests; the lint-only `scripts/report-action-contract.js` is the sole intentional set difference and is contract-checked. | Add JSDoc and checkJs coverage with each newly touched adapter boundary, keeping the allowlist and tests synchronized. | The remaining unchecked client files are exactly the five side-effect runtimes (`appShell`, `main`, `online`, `storage`, `ui`); adding them requires staged dependency separation, not ambient-global declarations or an allowlist-only change. CPU behavior is protected by fixed decision and 2–10 player self-play baselines. |

### Low

| Item | Classification | Risk | Impact | Suggested action | Deferred reason |
| --- | --- | --- | --- | --- | --- |
| Docs overlap across `AI_HANDOFF`, `POST_IMPLEMENTATION_AUDIT`, and operations docs | Now acceptable | Some historical notes duplicate current state. | AI may read stale historical notes before current guardrails. | Prefer adding concise current-state pointers, not rewriting old audit history. | Large docs cleanup is low value and can hide useful history. |
| PWA cache strategy is conservative but scattered | Touch carefully | Install-prompt ownership is isolated in `js/pwaShell.js`, but `sw.js`, inline update wiring, release tests, and model-loading docs still share update/cache behavior. | Over-broad cache edits can break update banner or RL model loading. | Keep current tests; update only for concrete PWA bug reports. | PWA cache strategy rewrite is explicitly out of scope. |
| RL docs/artifacts volume | Touch carefully | Registry, portfolio, scripts, and generated artifacts can be confused. | AI may review generated output instead of source/tests. | Continue using registry/tests as source of truth. | RL portfolio decisions are out of scope. |

## Classification By Action

### 1. Safe To Fix Now

The 2026-08-03 parity harness extraction keeps room construction and live/shadow/adoption assertions reusable; the 2026-07-28 re-audit completed the shared snapshot serializer, shared action dispatcher, detached transition seam, version codec/capability transport, and an off-by-default real-action shadow comparison. The next AI-safe work is evidence-first migration preparation, not a live cutover:

- Keep the all-action detached shadow parity synchronized with Action Contract additions and add richer rule-path fixtures whenever a touched action gains new state-dependent behavior.
- Preserve the now-explicit client/server hydrate policy adapters and their malformed/legacy contracts while expanding parity.
- Keep the default live wire legacy. The separately gated v1 Action wire, v1 rejoin/compacted-Snapshot wire, and recreate outer/nested wire may be exercised in automated/staged environments. Local `savedGame` remains the exact legacy rollback authority; the independent default-OFF v1 shadow may be exercised automatically. Recreate decode returns legacy Snapshot/action-log shapes before existing restore authority runs.

### 2. Requires Design Judgment

- Production durable provider, retention/privacy, process-safe locking, and secret rotation/activation policy (the provider-neutral contracts are implemented). Database/provider selection is explicitly deferred because recurring cost is not currently approved.
- Server socket handler decomposition beyond the migrated lobby/rejoin/gameAction families.
- Reconnect callback/write authority or room gate redesign beyond the gated event-state reads, rejoin request plan/effect executor, terminal cleanup decision/effect executor, and `js/onlineStorage.js` facade.
- CPU scoring/selection architecture changes beyond the extracted build execution boundary.
- Production activation or expansion of the default-OFF server-mirror pure-transition authority, production activation of the default-OFF recreate-request v1 wire, and production activation of the separately gated local-save v1 shadow.

### 3. Requires Real-Device Verification

- iPhone Safari modal/focus/inert/pointer behavior.
- Android Chrome/TWA install/update and background/resume behavior.
- Long online matches with reconnect, host migration, CPU turns, undo, and PWA update prompts.
- Screen reader announcement quality for live regions and modal transitions.

### 4. Better Not To Touch Now

- PWA cache strategy rewrite without a production update bug.
- Ad placement, public URL metadata, or domain policy changes.
- CPU strength / RL portfolio decisions without benchmark intent.
- Save format migrations from card name to card id without a compatibility plan.
- Broad docs-history cleanup that does not change current operational guidance.

### 5. Future Large Tasks

- Concrete durable canonical provider and production activation using the implemented adapter/priority contracts.
- Durable/signed restore authority beyond the implemented provisional quorum policy.
- Multi-room resume UI with candidate classification and stale/live/completed retention policy.
- Remaining recreate validation/signing/Socket/mirror effect split, plus narrowly scoped HTTP wiring only when a concrete route change requires it.
- CPU/RL architecture split with parity benchmarks and no-strength-change gates.

## Next Best Bets

Continue batch-by-batch re-auditing for small contract-first boundaries. The known production authority cutovers still need manual evidence or explicit rollout judgment:

1. Keep all reconnect event/effect/timer/callback authority flags off by default in production while staged evidence is collected. Automated parity now covers input-gate reads, the compatibility boolean, timer handle/deadline, timeout decisions, ACK-timeout plan and ordered effect executor, incoming gameAction/actionAccepted plans and their malformed-wire/apply-failure/gap/no-game recovery executors and successful commit executor, socket connect/disconnect plans and ordered executors, host-change plan and ordered executor, rejoin request plan and ordered emit executor, terminal cleanup decision and ordered executor, socket-disconnected/restore-lifecycle/normal retry-exhausted status effects, and restore queue flush plan selection and ordered execution, enqueue/overflow/generation-carry/drain/failure-suffix/clear transitions, detached shadow-store read/write parity, plus restore-abort plan selection and ordered effect execution, with legacy fallback; production Socket emit authority, session read, restore queue ownership/abort/handler-flush authority, production restore-abort/rejoin callback authority, other status contexts, storage, protocol, and production cleanup authority remain unchanged. Additional real-device online/PWA evidence is deferred because no further iPhone test device is available.
2. Keep `npm run report:action-contract` issue-free and add behavior coverage before the next action/pending path change.
3. Keep the typed Engine/Snapshot/repository/reconnect/CPU/UI adapter contracts, the 198-runtime no-emit checkJs allowlist, and the 199-file scoped ESLint gate synchronized; expand them only by stable adapter boundary, not by repository-wide cleanup.

## 2026-07-15 B分類オンライン耐障害化

- B1: ACK timeout、pending保護、restore queue上限、再接続世代管理、durable非依存の2/4クライアントE2E、短縮soak、Ubuntu WebKit nightly。GitHub Actions `29348807863` と Release `29348809695` は成功。
- APKのsecret不要validation-onlyはActions `29379796044`で成功。署名付きAPKは`ANDROID_KEYSTORE_BASE64`、`KEYSTORE_STORE_PASSWORD`、`KEYSTORE_KEY_PASSWORD`の設定後に`build_signed=true`で実行するため、実APKは引き続き人間待ち。
- manual online deliveryは固定production originへのGETと読み取り専用Socket.IO handshakeだけに限定し、Actions `29379820494`で成功。定期scheduleは追加しない。
- B3: file durable canonical storeは`review/durable-canonical-experimental`だけに隔離。既定storeは`noop`を維持し、canonical transactionとrestart persistenceはmainへ入れない。
- B4: 現行action ID境界とrolling compatibilityを`docs/PROTOCOL_COMPATIBILITY.md`で固定。dotted stream ID、watermark、非host canonical置換の実装は採用しない。
- 2026-07-18にAndroid 2台＋iPhone 2台の4人オンライン戦を、再接続ありで勝利まで完走確認。host移譲、server restart restore、Undo、online CPU、background復帰、PWA更新は未確認で、自動WebKitやこの1試合から完了を推定しない。

## 2026-07-29 Current cycle decisions

- Database-backed durable canonical state is deferred at the user's request because it introduces recurring cost. The default canonical store remains `noop`; no experimental durable branch code was imported.
- Additional real-device verification was waived for this cycle because an iPhone is not available. The existing 2026-07-18 mixed Android/iPhone completion remains the only direct mobile evidence.
- Safe non-device work completed: gameAction/disconnect handler extraction, event-labelled reconnect observation plus pure event-reducer/projection parity and gated UI/send/CPU/human input reads with legacy fallback, reporting HTTP pure policy extraction, expert lookahead-gate and simulation-stock extraction, Business Center candidate/scoring/random-simple selection extraction, modal visibility/focus policy extraction, default-OFF modal-open and post-hide modal-close plan/effect extraction, full player-panel HTML extraction, card-select state/view extraction, local/online-player-setting normalization/view extraction, online lobby-button/status view extraction, injected PWA install controller extraction, one shared client storage owner across app shell/main/online/storage/UI/stats, lifecycle start/dedupe pure contracts, expanded pure watchdog snapshot/phase/modal policy, separately gated versioned live Action and rejoin/compacted-Snapshot wires, shared local-save serialize/hydrate delegation plus default-OFF rollback-safe v1 shadow dual-write with the exact legacy authority retained, and typed Engine/Snapshot/repository/reconnect adapter boundaries.

## 2026-08-03 Batch 18 maintenance boundaries

- `js/cpuEvaluation.js` now owns the stable best-landmark-candidate reduction after `CPU.js` computes the unchanged eligibility, urgency, threshold penalty, reserve, and score values. Highest score wins, equal scores prefer the lower cost, and complete ties keep input order; focused wrapper and self-play regression tests guard CPU strength.
- `js/onlinePayload.js` now owns construction of the pending outbound action entry. `online.js` still performs sequence allocation, client-action ID generation, memory/scoped legacy-compatible storage writes, ACK handling, reconnect, and Socket.IO effects in their original order and format.
- `js/uiPendingMenu.js` now owns the detached Business Center selection view for group reset, selected-chip ARIA/class state, and hidden-input value. `js/uiPendingEffects.js` now owns the exact DOM mutation order while `ui.js` retains DOM lookup and the public `bcSelectCard()` wrapper.
- `server/staticAssets.js` now registers index, root-file, and static-directory content routes through an injected adapter. The same module now also registers asset-links, Service Worker, and version metadata routes before reporting; `server.js` retains injected values and global route ordering. No cache, PWA, URL, or middleware behavior changed.
- These are maintenance seams, not authority migrations. Remaining broad CPU orchestration, online reconnect/restore ownership, modal lifecycle effects, Socket handlers, durable canonical state, and production authority activation retain their existing deferrals and gates.

## 2026-08-03 Batch 19 maintenance boundaries

- `OnlineRuntimeFlags.createReader()` now owns dynamic runtime-root resolution for all default-OFF schema/reconnect/Engine authority flags. Existing global wrapper names and strict boolean-`true` activation remain unchanged.
- `LifecycleNotify` now owns current/legacy notification keys, fallback reads, boolean-string writes, start-marker reads/writes, and marker deletion through an injected storage accessor. `appShell.js` retains lifecycle state, runtime metadata, transport, checkpoints, and public APIs.
- `registerStaticMetadataRoutes()` complements the existing static-content adapter and preserves asset-links → Service Worker → version registration before reporting routes, including all prior content types and no-cache headers.
- `UiPendingMenu` owns Business Center selection view, `UiPendingEffects` owns ordered DOM application, and `ui.js` owns element lookup/public dispatch. Missing-input behavior and reset-before-select order are direct contracts.
- Scoped static gates now cover 174 maintenance files and 173 checkJs runtime files. The five intentionally excluded side-effect runtimes remain unchanged.


## 2026-08-03 Batch 20 architecture boundaries

- `js/pendingActionQueue.js` now owns the pending field/action contract, input-nonmutating queue normalization, legacy-field reconstruction, consecutive grouping, and serialization. `GameManager` retains pending counters, phase authority, queue mutation during enqueue/consume/clear, and its existing public/static APIs; browser, server mirror, integration, and self-play loaders fix the new dependency order.
- `server/lobbyAdmission.js` now owns pure create-room landmark/human-host selection and join-room started/socket/name/seat admission. `server/lobbySocketHandlers.js` retains payload/schema validation, rate limits, room/socket mutation, token generation, Socket.IO event names and exact emit/start-check order.
- `js/cpuSelection.js` now owns no-consumption empty random choice, first-on-tie maximum reduction, and lexicographic first-on-tie reduction. The v2simple TV target path uses these primitives while `CPU.js` retains candidate generation and every heuristic value. Exact decision and 2–10 player self-play baselines prove unchanged strength and RNG behavior.
- `js/uiTabEffects.js` now applies main-tab, online-tab, and online-availability views in the existing DOM mutation order. `UiTabView` remains pure; `ui.js` and `appShell.js` retain DOM lookup, stats triggering, event/lifecycle orchestration, and public APIs.
- Scoped gates now cover 178 ESLint maintenance files and 177 checkJs runtime files. The same five side-effect client runtimes remain deliberately outside checkJs.
- This batch does not activate Engine/schema/reconnect/modal authority, alter Socket.IO payloads, change save/storage formats, modify PWA cache policy, or change CPU tuning.


## 2026-08-03 Batch 21 architecture boundaries

- `js/gameTurnPolicy.js` now owns the pure income-complete phase projection, the exact amusement-park extra-turn predicate, and circular next-player arithmetic. `GameManager` retains dice generation, income/effect mutation, turn reset, logging, and public rule methods. Direct policy tests plus the complete GameManager and shared-Engine shadow suites preserve the current rules.
- `OnlineRuntimeFlags.createNamedReaders()` now projects 49 reconnect/Engine authority readers from the frozen 53-name registry. Root lookup remains per-call, only boolean `true` enables a flag, unknown names fail before runtime use, all existing global reader names remain available, and the four schema transport readers retain their codec prerequisites.
- `server/roomGcRuntime.js` now owns only the 10-minute timer registration, callback-time clock read, and optional `unref()` effect. `roomLifecycle` retains TTL policy and room deletion; `server.js` supplies the live rooms object. The Socket.IO protocol, room TTLs, and cleanup mutation are unchanged.
- `js/uiWinnerEffects.js` now owns the ordered winner-screen runtime/DOM effect boundary: status HTML, first-presentation marker/sound/stats/lifecycle notification, save/session cleanup, online/PWA completion, controls, and dependent redraws. `UiWinner` remains the pure HTML view and `ui.js` retains live state/DOM lookup and injected callbacks. Initial and repeated render orders are direct contracts.
- Current scoped gates cover 181 ESLint maintenance files and 180 checkJs runtime files. The same five side-effect client runtimes remain deliberately outside checkJs.
- No game rule, CPU strategy/tuning, Action/Snapshot/save schema, localStorage key, Socket.IO event/payload, reconnect authority, PWA cache policy, or production rollout flag changed in this batch.

## 2026-08-04 Batch 22 architecture boundaries

- `js/pendingActionQueue.js` now owns input-nonmutating enqueue, consume, and clear transition plans in addition to pending field/action normalization. `GameManager` remains the mutation and phase authority and applies those plans through one compatibility adapter; malformed legacy queue entries keep their existing representation.
- `CPUSelection.stableRankDescending()` now owns score-once descending ranking with explicit original-order ties. Three existing scored candidate sorts delegate to it. Candidate construction, heuristic values, RNG calls, and action execution remain unchanged; fixed decision and 2–10 player self-play baselines matched exactly.
- `server/processRuntime.js` now owns process exception subscription and HTTP listen wiring through injected process/server/logger adapters. `server.js` retains initialization and dependency composition; event names, host, raw port value, listen timing, and log text are unchanged.
- Active-game rendering now follows `UiGameStatusView` → `UiGameStatusEffects` → `ui.js`: the view remains pure, the effect boundary fixes control/redraw/coin/synchronization order, and `ui.js` retains live state, DOM lookup, optional runtime callbacks, and `safeRenderStep` recovery.
- Current scoped gates cover 183 ESLint maintenance files and 182 checkJs runtime files. The five side-effect client runtimes remain intentionally outside checkJs; this batch did not change rules, CPU strength, save/storage formats, Socket.IO protocol, reconnect authority, or PWA cache policy.

## 2026-08-04 Batch 23 architecture boundaries

- `js/gameBuildPolicy.js` now owns ordered, input-nonmutating card/landmark build admission plans. Lazy facts preserve the previous fail-fast read order. `GameManager` retains player/card/landmark mutation, clone behavior, loan income, structured logs, phase authority, and public methods.
- `GameTurnPolicy` now owns next-turn admission, airport-award eligibility, and IT-pending versus advance decisions. `GameManager` still performs winner lookup, airport coin/log effects, active IT-card lookup, pending mutation, and turn advancement in the original order.
- `js/onlineSocketRegistry.js` is the frozen contract for all 18 existing inbound client Socket events, including injected hostless names and `appError`. It enforces unique names, registration order, handler completeness, and fail-before-register validation while `online.js` retains every callback and reconnect/ACK/restore effect.
- `js/clientEventRuntime.js` now owns injected browser registration for crash/error and online/offline events. `appShell.js` retains reporting, crash UI, console interception, idempotence flags, connectivity rendering, PWA bindings, and watchdog ownership.
- Current scoped gates cover 186 ESLint maintenance files and 185 checkJs runtime files. The same five side-effect client runtimes remain deliberately excluded. No rule outcome, CPU policy/strength, save/storage format, Socket.IO event/payload, reconnect authority, or PWA cache behavior changed.

## 2026-08-04 Batch 24 architecture boundaries

- `GameTurnPolicy.planItResolution()` now owns the frozen `rejected` / `saved` / `insufficient` / `skipped` IT-resolution transition plan. `GameManager.resolveIT()` remains the mutation/log/turn executor, including the legacy insufficient-coin advance behavior.
- `CPUSelection.stableRankLexicographic()` now owns score-once, per-key ascending/descending ranking with explicit original-order ties. Six precomputed landmark candidate sorts delegate to it; candidate construction, heuristics, RNG, profiling-sensitive evaluations, and action execution remain unchanged. Exact decision and 2–10 player self-play baselines matched.
- `server/socketConnectionRuntime.js` now owns the `connection` subscription, connection log, and exact hostless → lobby → action → rejoin → recreate → disconnect family registration order. `server.js` remains the composition root and supplies every existing handler dependency; event names, payloads, callbacks, room authority, and restore behavior are unchanged.
- `js/uiRenderRuntime.js` now projects no-game/winner/active render state and fixes effect order. Winner rendering stops before active rendering/persistence; active rendering persists only after the existing render callback. `ui.js` retains live game reads, DOM/runtime callbacks, crash handling, and public APIs.
- Current scoped gates cover 188 ESLint maintenance files and 187 checkJs runtime files. The same five side-effect client runtimes remain deliberately excluded. No rule outcome, CPU strength, save/storage format, Socket.IO protocol, reconnect authority, or PWA/SW behavior changed.


## 2026-08-04 Batch 25 architecture boundaries

- `js/gameDicePolicy.js` now owns deterministic roll routing, station selection admission/outcome, radio-tower reroll admission, Harbor choice/income resolution, and dice-outcome formatting plans. `GameManager` retains RNG consumption, live mutation, income/card effects, structured logs, and public rule methods; direct, shared-Engine, local shadow, and 2–10 player schema parity tests preserve current rule behavior.
- `CPUSelection.stableRankAscending()` and `stableRankDescending()` now share the same stable lexicographic primitive. Three remaining single-key, precomputed candidate sorts delegate without changing candidate construction, heuristic values, tie order, or evaluation count. Profiling-sensitive opponent-threat sorts remain local; decision and 2–10 player self-play baselines matched exactly.
- `server/roomProjection.js` now owns read-only lobby/game-start projections: displayed names, human slots, shuffled order, client versions, reconnect-token hashes, and hostless capability metadata. `roomLifecycle` retains TTL/rate/admission/connected-player and host-epoch state decisions; `server.js` retains composition and its existing public helper surface.
- Current scoped gates cover 190 ESLint maintenance files and 189 checkJs runtime files. The five side-effect client runtimes remain deliberately excluded. No game rule, CPU strength, save/storage format, Socket.IO event or payload, reconnect authority, hostless policy, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 26 architecture boundaries

- `CpuTurnStrategy` now requests a canonical build proposal through `CPU.chooseBuildAction()`, and `CPU.executeBuildAction()` owns application of that already-selected proposal. `CPU.build()` remains the compatibility entry point; the live scheduler uses the action-only path when available and keeps the legacy fallback for existing test/double implementations.
- `js/onlineActionLog.js` now owns the pure append/compaction transition plan and its explicit ordered executor. `online.js` retains live state reads, snapshot construction, storage callbacks, sequence allocation, Socket.IO/ACK/reconnect authority, and all existing keys and wire formats.
- `server/restoredRoomRuntime.js` now composes restored-room activation, delivery, and completion through injected effects. The existing default-OFF activation/delivery authority flags, hostless rejection, persistence and Socket delivery order, event names, payload shape, and public `{ ok, reason }` failure result remain unchanged.
- Current scoped gates cover 192 ESLint maintenance files and 191 checkJs runtime files. The five side-effect client runtimes remain deliberately excluded. No game rule, CPU tuning/strength, save or localStorage format, Socket.IO protocol, reconnect/hostless policy, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 27 architecture boundaries

- `resolveExpertProfileTuning()` in `js/cpuTuning.js` now owns expert preset/profile/simulation-mode composition as an input-nonmutating policy. `CPU.js` retains profile selection, active-field assignment, all heuristics, candidate generation, and action choice. Exact decision and 2–10 player self-play baselines matched.
- `js/gameCardActivationPolicy.js` now selects dormant cards eligible to revive for a die result in existing Set/input order and only evaluates the color predicate after dice matching. `GameManager` retains card revival mutation, structured logs, income color/effect order, RNG, pending actions, and every rule outcome.
- `js/onlineSessionLifecycle.js` now fixes completed/reset transition plans and their full ordered state/effect sequence. `online.js` retains live state variables, Socket disconnect, storage deletion, retry/queue owners, reconnect event reducer, public APIs, and existing room-scoped key behavior.
- Current scoped gates cover 194 ESLint maintenance files and 193 checkJs runtime files. The five side-effect client runtimes remain excluded. No rule result, CPU strength, save/localStorage shape, Socket.IO protocol, reconnect authority/timeout, PWA/SW behavior, or production flag changed.


## 2026-08-04 Batch 28 architecture boundaries

- `scripts/check-static-files.js` now discovers tracked and unignored untracked JavaScript/JSON files once and parses all of them in one Node process. It preserves the previous repository scope, JSON rejection, JavaScript syntax rejection, and shebang acceptance while removing per-file process startup. Shell, Python, ESLint, and checkJs gates remain unchanged.
- `server/gameStartAdmission.js` owns the reasoned missing/already-started/waiting/start decision, and `server/gameStartRuntime.js` owns the validated `mark → gameStart emit → log` order. `gameStartCoordinator` retains room lookup, lazy human-slot acquisition, payload construction, and composition. Event names, payloads, player requirements, room mutation, and persistence remain unchanged.
- `js/uiTutorialSettings.js` owns frozen enabled/level/cycle transitions and the ordered state → existing-key storage → controls → render boundary. `ui.js` retains live state variables, DOM lookup, public functions, and rendering. Existing `tutorialEnabled` / `tutorialLevel` keys, boolean strings, invalid-level fallback, and PWA asset behavior remain unchanged.
- Current scoped gates cover 198 ESLint maintenance files and 197 checkJs runtime files. The five side-effect client runtimes remain excluded. No game rule, CPU behavior, save/storage format, Socket.IO protocol, reconnect authority, or PWA cache policy changed.


## 2026-08-04 Batch 29 architecture boundaries

- `tests/run-all.js all` now keeps one isolated Node process per test while scheduling two processes by default. Results are buffered and emitted in declared order, every test still runs after a failure, non-`all` groups remain sequential, and `MACHIKORO_TEST_CONCURRENCY=1` is the rollback path. Concurrency is bounded to 1..8 and contract-tested.
- `js/gamePendingResolutionPolicy.js` now owns lazy, reasoned admission for TV, Business Center, Cleaning Company, Moving Company, and Renovation Company resolution. `GameManager` retains card/player lookup adapters, all state mutation, coins, dormancy, logs, pending queue consumption, and phase changes. Shared client/server runtime loader order is explicit.
- Runtime dependency tests now validate every adjacent edge in declared dependency chains instead of silently checking only the first pair. The self-play runtime includes the pending policy before `GameManager`, preventing browser/server/self-play loader drift.
- `CPULegalMoves.lookaheadStrongOpponentIndexes()` now owns expert lookahead opponent generation for leader-only, next-seat-only, top-two, and all-opponent modes. `CPU.js` retains flag precedence, threat evaluation, CPU construction, simulation, and every tuning value. Fixed decisions and the full 2–10 player self-play baseline matched.
- Current scoped gates cover 199 ESLint maintenance files and 198 checkJs runtime files. No game outcome, CPU strength, save/storage shape, Socket.IO protocol, reconnect authority, or PWA behavior changed.

## 2026-08-04 Batch 30 architecture boundaries

- `GamePendingResolutionPolicy.completionTransition()` now owns the deterministic pending-complete decision and target build phase. `GameManager` applies the plan and retains pending mutation, queue consumption, logging, and live phase assignment; remaining pending work deliberately produces no phase write, matching the legacy behavior.
- `js/gameCoinTransaction.js` now owns frozen multi-player collection and equal-distribution plans. Stadium, Publisher, Tax Office, IT Startup, and Park use those plans, while `GameManager` retains activation order, request calculation from cards/landmarks, player mutation, and structured logs. Direct transaction contracts and existing 2–5 player rule tests preserve caps, totals, remainder ownership, and outcomes.
- `OnlineRetryPolicy.createActionFlightController()` now owns the ACK-flight flag, start timestamp, and timeout handle as one state boundary. `online.js` mirrors its snapshot into the existing globals used by app-shell diagnostics and retains ACK/reconnect effects, Socket.IO calls, pending-action persistence, and timeout policy. Timeout duration and all wire/storage contracts are unchanged.
- Current scoped gates cover 200 ESLint maintenance files and 199 checkJs runtime files. The five side-effect client runtimes remain deliberately excluded. No rule result, CPU strength, save/localStorage shape, Socket.IO protocol, reconnect authority/default flag, PWA/SW behavior, or production rollout changed.

## 2026-08-04 Batch 31 architecture boundaries

- `GameCoinTransaction.sequentialCollectionPlan()` now owns ordered balance-capped red-card collection. `GameManager` still performs dormant revival, activation/landmark/category admission, per-opponent ordering, player mutation, and exact French Restaurant/Members Bar/generic logs. Direct plans and a two-Members-Bar rule contract preserve first-card priority and zero-coin later activation.
- `CPUEvaluation.turnScorePair()` now owns one-die and Station two-dice weighted aggregation. `CPU.js` retains temporary focus-player selection, roll-score caching, card/rule evaluation, profiling, and strategy decisions. Dice read order, fixed decisions, and self-play remain unchanged.
- `server/hostlessRestoreApproval.js` now composes normalized room absence, approved recreate invocation, and provisional-room confirmation through injected dependencies. `server.js` retains rooms, recreate authority, Socket callbacks, candidate coordinator/gateway, protocol, and the existing public approval function name.
- `UiPendingMenu` continues to own the pending-modal style view and `UiPendingEffects.applyModalInteraction()` now owns modal → inner → content DOM application order. `ui.js` retains live lookup, re-entry protection, blocking-modal admission, violations, content writes, and public render flow.
- Current scoped gates cover 201 ESLint maintenance files and 200 checkJs runtime files. The five side-effect client runtimes remain deliberately excluded. No rule result, CPU strength, save/storage shape, Socket.IO protocol, hostless authority, reconnect default, modal behavior, or PWA/SW behavior changed.

## 2026-08-04 Batch 32 architecture boundaries

- `GameCardActivationPolicy.blueIncomePlan()` now owns the frozen blue-card activation and income result for Cornfield, Harbor, Tuna Boat, and ordinary blue cards. Lazy landmark-count and Tuna dice facts preserve the existing admission/RNG read order. `GameManager` retains dormant revival, color/dice filtering, player mutation, structured logs, RNG adapter ownership, and activation order.
- `js/onlineRestoreLifecycleState.js` now owns restore generation, in-progress, and quarantine state as one controller. `online.js` mirrors controller snapshots into the existing diagnostic/compatibility variables and preserves the exact GAME_START, REJOIN_DATA, disconnect, abort, queue-flush, and reset effect order. Reconnect authority, retry timing, queue policy, storage, and Socket.IO payloads remain unchanged.
- Crash recovery now follows `CrashScreen` view/focus plan → `CrashScreenEffects` DOM application → `appShell.js` lifecycle orchestration. The effect boundary owns visibility, accessibility attributes, initial focus, focus trapping, and hide operations; app-shell retains CPU cancellation, listener lifecycle, saved-game lookup, reporting, and resume dispatch.
- Card effect registration coverage now accepts explicit rule ownership in either `GameManager` or the injected `GameCardActivationPolicy` boundary, while continuing to require CPU coverage for every non-normal effect. This keeps the omission detector effective after safe delegation.
- Current scoped gates cover 203 ESLint maintenance files and 202 checkJs runtime files. The five side-effect client runtimes remain deliberately excluded. No game outcome, CPU strength, save/localStorage shape, Socket.IO protocol, reconnect authority/default, crash recovery behavior, or PWA/SW cache policy changed.

## 2026-08-04 Batch 33 architecture boundaries

- `GameCardActivationPolicy.greenActivationPlan()` now owns the frozen Winery, Moving Company, Loan Office, Renovation Company, and ordinary green-card outcome selection. Lazy income and renovation-target facts preserve existing read order. `GameManager` retains revival, dice/color admission, mutation, pending fields, dormancy, logs, and activation order.
- `OnlineRestoreLifecycleState` now owns restore queue-flush activity together with generation, in-progress, and quarantine state. `online.js` keeps the existing compatibility projection and exact flush/reset effect order; reconnect authority, queue contents, retry timing, storage, and Socket.IO payloads are unchanged.
- `CPUEvaluation` now owns progress-income card eligibility and ordered aggregation. `CPU.js` retains state-cache ownership, card value/frequency callbacks, every heuristic and tuning value, and action selection. Focused CPU contracts, type checks, lint, and the fixed decision baseline preserve current behavior.
- Scoped gates remain 203 ESLint maintenance files and 202 checkJs runtime files. No game result, CPU strength, save/localStorage format, Socket.IO protocol, reconnect authority/default, or PWA/SW behavior changed.

## 2026-08-04 Batch 34 architecture boundaries

- `GameCardActivationPolicy.purpleActivationPlan()` now owns frozen purple-card effect dispatch, pending-field selection, and Business Center/Cleaning target availability. Lazy target facts preserve legacy read order. `GameManager` retains all coin transactions, pending mutation, logs, revival, and activation order.
- `CPUEvaluation.receivedCardValue()` and `ownedCardValue()` now own special-effect dispatch and transfer/ownership arithmetic. `CPU.js` retains card-income adapters, dice frequency, dependency evaluation, copy ordinals, all heuristic values, caches, candidate generation, and action selection.
- `js/localResumePreloadState.js` now owns RL preload pending state, generation, and stale-completion rejection. `storage.js` projects pending state to the existing resume button and retains repository reads, RL model calls, hydration, notices, and resume effects. Script/PWA cache order is contract-checked.
- Scoped gates now cover 204 ESLint maintenance files and 203 checkJs runtime files. Whole-file `storage.js` checkJs remains deferred: direct admission exposes more than 100 ambient browser-global errors, so future progress must move typed boundaries out rather than add suppressing ambient declarations.
- No game result, CPU strength, save/localStorage format, Socket.IO protocol, reconnect authority/default, or PWA/SW cache policy changed.

## 2026-08-04 Batch 35 architecture boundaries

- `GameCardActivationPolicy.redActivationPlan()` now owns frozen Harbor Sushi, French Restaurant, Members Bar, and ordinary red-card admission/kind/requested-amount decisions. Landmark, coin, and Mall category facts remain lazy. `GameManager` retains opponent/card order, revival, balance-capped collection, mutation, and exact logs.
- `js/localSaveRuntime.js` now owns local-save admission and the serialize → repository-save exception boundary. `storage.js` retains live game/settings adapters and the exact legacy payload fields; winner lookup still occurs after no-game/online short circuits and outside the swallowed persistence-error boundary.
- `js/uiWatchdogReporting.js` now owns checkpoint → recovery → snapshot storage → client-report ordering and message construction. `appShell.js` retains freeze observation/classification, live recovery handlers, storage/report adapters, and watchdog scheduling.
- Scoped gates now cover 206 ESLint maintenance files and 205 checkJs runtime files. The same five side-effect runtimes remain excluded as whole files while typed boundaries continue moving out.
- No game result, CPU strength, save/localStorage format, Socket.IO protocol, reconnect authority/default, watchdog recovery classification, or PWA/SW cache policy changed.

## 2026-08-04 Batch 36 architecture boundaries

- `js/gamePendingTransition.js` now owns detached outcome plans for TV coin transfer, Business Center exchange indexes/dormancy inheritance, Cleaning target enumeration, Mover transfer/reward, and Renovation reward. `GameManager` retains admission, live object mutation, pending queue consumption, phase changes, and structured logs in their original order.
- `OnlineRetryPolicy.createRejoinAttemptController()` is the sole write owner for rejoin attempt count and exhaustion. `online.js` keeps compatibility projections for existing diagnostics while timer/callback/Socket/retry behavior, the 8-attempt limit, and production authority defaults remain unchanged.
- `server/runtimeLimits.js` is the frozen contract for action-log, room-lifecycle, Socket/restore payload, client-error, and game-lifecycle limits. `server.js` remains the composition and public-export layer; every numeric limit and rejection boundary is unchanged.
- Scoped gates now cover 208 ESLint maintenance files and 207 checkJs runtime files. Whole-file client exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

These are deterministic rule-transition, explicit online state ownership, and server configuration boundaries. They do not change game rules, CPU policy/strength, Action/Snapshot/save schemas, localStorage keys, Socket.IO events/payloads, reconnect timing, PWA/SW behavior, or rollout defaults.

## 2026-08-04 Batch 37 architecture boundaries

- `CPUSimulation.cloneGame()` now owns detached simulation-runtime reconstruction, including card cloning, dormant-card identity remapping, landmark/pending/turn fields, and legacy pending-queue rebuilding. `CPU.js` retains profiling and injected runtime adapters; exact decisions and 2–10 player self-play remain unchanged.
- `js/onlinePendingOutboundState.js` is the room-scoped in-memory owner for pending outbound actions. `online.js` retains action creation, current-session admission, legacy/scoped localStorage writes and fallback, ACK matching, reconnect, and Socket.IO transport.
- `ClientReporting.createAdmissionController()` now owns client-error dedupe key/time state and its injected clock. `appShell.js` retains report context/building, suppressed checkpoints, and transport composition; the 10-second suppression boundary is unchanged.
- Scoped gates now cover 209 ESLint maintenance files and 208 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

This batch advances reproducible CPU simulation, explicit online state ownership, and a thinner app-shell without changing CPU choices/RNG, storage keys or payloads, Socket events, reporting payloads, PWA behavior, or authority defaults.


## 2026-08-04 Batch 38 architecture boundaries

- `GameBuildPolicy` now owns frozen card and landmark build-result transitions after its existing admission plans. `GameManager` retains live player/card/landmark mutation, clone identity, Loan Office log order, and public rule methods; focused and shared-Engine regressions preserve outcomes.
- `server/existingRoomRestoreRuntime.js` now owns existing-room restore admission, replace pass-through, legacy rejoin fallback, and optional effect-authority dispatch. `server.js` remains the composition root; reconnect identity, host reselection, persistence, `rejoinData` / `playerRejoined` event names, payloads, and default-OFF authority behavior are unchanged.
- `UiCardSelect.createSelectionController()` now owns detached card/landmark selection state for modal interactions. `ui.js` keeps compatibility projections for save/online restore writers, DOM rendering, and browser-global APIs; required cards, the one-landmark minimum, selection order, and render timing are unchanged.
- The new server runtime is explicitly covered by both scoped gates. Current scope is 210 ESLint maintenance files and 209 checkJs runtime files; whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

This batch advances deterministic rule transitions, a thinner server composition root, and explicit UI state ownership without changing rules, CPU strength, save/localStorage formats, Socket.IO protocol, reconnect rollout defaults, or PWA/SW behavior.


## 2026-08-04 Batch 39 architecture boundaries

- `GameTurnPolicy.pendingResetState()` and `turnResetPlan()` now own detached pending/turn reset outcomes, including conditional log/dice clearing. `GameManager` remains the live mutation owner and clones the frozen pending queue before use; constructor, next-turn, schema parity, and local Engine shadow behavior are unchanged.
- `CPUEvaluation.expertPositiveIncomeCap()` now owns all hard/soft/remaining-landmark cap modes with lazy cost/coin facts. `CPU.js` retains v2simple admission, active tuning mode, game/player adaptation, and every caller; fixed decisions and the complete 2–10 player self-play baseline remain identical.
- `LifecycleNotify.createController()` is now the sole owner of app-shell lifecycle session/start/finish state. `appShell.js` retains runtime metadata, storage marker, transport, checkpoints, public APIs, and the exact duplicate/reload-suppression effect order.
- Scoped gates remain 210 ESLint maintenance files and 209 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

This batch advances deterministic shared-Engine state, action-only reproducible CPU evaluation, and a thinner app-shell without changing game rules, CPU choices/RNG, save/localStorage formats, Socket.IO protocol, notification payloads, PWA/SW behavior, or rollout defaults.

## 2026-08-04 Batch 40 architecture boundaries

- `OnlineActionSequence.createController()` is now the sole mutable owner of the last-applied online action sequence. `online.js` retains snapshot/log/storage reads, ACK and replay effects, reconnect flow, and Socket.IO transport; sequence calculation and every wire/storage shape are unchanged.
- `server/newRoomRestoreRuntime.js` now owns new-room restore preparation, activation, and delivery sequencing. `server.js` remains the composition root and keeps existing-room handling, candidate authority, room mutation dependencies, event names, payloads, and default rollout behavior unchanged.
- `UiLogDisplay.createHistoryController()` now owns detached log entries and the previous rendered length. `ui.js` retains text construction, rendering, scrolling, card-filter reset, and DOM/public API behavior.
- Scoped gates now cover 211 ESLint maintenance files and 210 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

This batch advances explicit online state ownership, a thinner server composition root, and isolated UI history state without changing rules, CPU strength, save/localStorage formats, Socket.IO protocol, reconnect authority/defaults, UI presentation, or PWA/SW behavior.

## 2026-08-04 Batch 41 architecture boundaries

- `GameTurnPolicy.incomeCompletionPlan()` now owns the immutable City Hall relief and post-income phase result. `GameManager` retains red/blue/green/purple activation order, live coin/log mutation, and phase assignment; City Hall ownership remains lazy and is read only at exactly zero coins.
- `js/onlineLobbyRequestState.js` is now the sole owner of create/join pending state, request kind, generation, and timeout handle. `online.js` keeps compatibility read projections, the unchanged 15-second timer, DOM notices, Socket emits, disconnect handling, and RL preload effects.
- `CrashScreen.createController()` now owns crash-screen shown/hidden state and duplicate-show admission. `appShell.js` retains CPU cancellation, saved-game lookup, DOM/focus effects, listener wiring, resume dispatch, and public browser APIs.
- Scoped gates now cover 212 ESLint maintenance files and 211 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

This batch advances deterministic Engine transitions, explicit online request state, and a thinner app-shell without changing rules, CPU strength, save/localStorage formats, Socket.IO events/payloads, timeout behavior, crash recovery, or PWA/SW behavior.

## 2026-08-04 Batch 42 architecture boundaries

- `CPUEvaluation.affordablePurchaseScore()` now owns the ordered composition of base gain, landmark synergy, spam/economy penalties, strong conditional and Renovation risk, role pressure, normal safety, and crowd adjustment. `CPU.js` remains the feature adapter and preserves every heuristic value, callback order, difficulty short circuit, candidate order, and RNG behavior.
- `GameTurnPolicy.winnerIndex()` now owns deterministic first-winner traversal. `GameManager.checkWinner()` maps the result back to the original player reference; enabled-landmark order, `Player.hasWon()` semantics, first-player tie behavior, and post-win Action closure remain unchanged.
- `UiModalPolicy.createConfirmController()` now owns confirm awaiting state and the optional cancel handler. `ui.js` retains DOM lookup, nested-modal admission, text/focus/inert effects, public callbacks, and the existing close-before-cancel-callback order.
- Scoped gates remain 212 ESLint maintenance files and 211 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

This batch advances action-only reproducible CPU evaluation, deterministic Engine outcome selection, and explicit UI state ownership without changing rules, CPU strength, persistence, protocol, modal behavior, or PWA/SW behavior.

## 2026-08-04 Batch 43 architecture boundaries

- `UiTurnAnnouncer.createTimerController()` now owns the two-stage show/hide timer handle and replacement cancellation. `ui.js` retains DOM lookup, text/style/class effects, and the public `showTurnAnnouncer()` API; the 1300 ms display and 400 ms transition timings are unchanged.
- `OnlineHostlessRestoreState.createController()` is now the sole owner of hostless-restore request pending state. `online.js` retains bundle validation, reconnect state, Socket.IO request/candidate/status effects, terminal handling, and all existing event names and payloads.
- A further `server.js` extraction was deferred in this batch because the inspected room name/slot/version/token/capability projections already belong to `server/roomProjection.js`; another wrapper would duplicate rather than clarify ownership.
- Scoped gates now cover 213 ESLint maintenance files and 212 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU strength, save/localStorage format, Socket.IO protocol, reconnect timeout/authority, UI presentation timing, or PWA/SW cache policy changed.

## 2026-08-04 Batch 44 architecture boundaries

- `GameTurnPolicy.turnAdvancePlan()` now owns the immutable repeat-turn/next-player outcome, player index, turn-count delta, roll phase, and reset options. `GameManager` remains the live mutation/log adapter; Amusement Park admission, exact log text, dice clearing, and player order are unchanged.
- `OnlineSchemaTransport.createSelectionController()` is now the sole owner of negotiated schema selection state. `online.js` retains GAME_START/REJOIN_DATA admission, runtime flags, action/snapshot/recreate codecs, Socket transport, and the default legacy path.
- `server/recreateRoomRuntime.js` now owns restore admission routing and the existing-room/new-room branch. `server.js` injects room lookup, error delivery, and the existing restore runtimes; validation, identity, room mutation, persistence, Socket events, and payload shapes remain in their prior owners.
- Scoped gates now cover 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU strength, save/localStorage format, Action/Snapshot schema, Socket.IO protocol, reconnect authority/timing, or PWA/SW behavior changed.

## 2026-08-04 Batch 45 architecture boundaries

- `CPUEvaluation.expertCardPenalty()` now owns the complete expert card-name/copy/landmark/player-count penalty table. `CPU.js` remains the player/game feature adapter and keeps built-landmark reads lazy for Renovation Company only; every numeric heuristic and branch order is unchanged.
- `UiBuildMenu.createFilterController()` is now the sole owner of build-card filter state. `ui.js` retains HTML generation, DOM rendering, public `setCardFilter()`, enabled-card/stock gates, and the existing render-on-every-selection behavior.
- `ClientEventRuntime.createBindingController()` now owns app-shell one-time binding state for crash reporting, console reporting, online status, freeze watchdog, and resize handling. `appShell.js` retains listener/effect registration and preserves all registration and repeated-call behavior.
- A combined `nextTurn` airport/IT plan was deferred because the legacy sequence applies and logs Airport income before reading the active IT Startup card. A safe future plan must represent the effect boundary explicitly rather than evaluate all facts before effects.
- Scoped gates remain 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No CPU heuristic or decision, game rule, save/localStorage format, Socket.IO protocol, reconnect authority/timing, listener order, UI rendering behavior, or PWA/SW behavior changed.

## 2026-08-04 Batch 46 architecture boundaries

- `GamePendingResolutionPolicy.hasBusinessExchange()` and `hasCleaningTarget()` now own the pure, short-circuiting target-availability traversal used by purple-card admission. `GameManager` remains the domain-object adapter and retains activation order, pending mutation, exact logs, card identity, and dormancy semantics.
- `CPUEvaluation.expertCrowdNormalPlan()` now owns the expert four-player normal-leaning predicate. `CPU.js` retains current-player, remaining-landmark, and stable-income feature adaptation; fact order, every threshold, fixed decisions, and the complete 2–10-player self-play baseline are unchanged.
- `UiGameStatusEffects.createTurnStateController()` now owns the previous-player index used for turn-announcer admission. `ui.js` retains active-game view construction, DOM/effect execution, reset timing, and browser-global APIs. Lazy controller creation preserves partial UI runtimes.
- Scoped gates remain 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/decision, save/localStorage format, Socket.IO protocol, reconnect authority/timing, UI rendering order, or PWA/SW behavior changed.

## 2026-08-04 Batch 47 architecture boundaries

- `GamePendingResolutionPolicy.resolveMinorCardRef()` now owns deterministic index/name lookup and major-card rejection for Business Center and Moving Company targets. `GameManager` remains the Player/category adapter and retains pending admission, mutation, dormancy transfer, card order, and exact logs.
- `CPUEvaluation.expertDisruptionScale()` now owns the complete expert self-race disruption multiplier table. `CPU.js` retains feature adaptation and flag lookup; focus-player selection, lazy remaining-landmark reads, every numeric value, fixed decisions, and the full 2–10-player self-play baseline are unchanged.
- `UiWatchdogMonitor.createPendingBatchController()` now owns post-build UI stabilizer pending/task-count state. `appShell.js` retains snapshot admission, the exact 0/250/1500/3500 ms schedule, recovery DOM effects, checkpoints, and fallback execution.
- Scoped gates remain 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice, save/localStorage format, Socket.IO protocol, reconnect authority/timing, UI recovery timing, or PWA/SW behavior changed.

## 2026-08-04 Batch 48 architecture boundaries

- `OnlineRestoreQueueState.createDiagnosticController()` now owns the five restore-queue store-read/store-write/state/effect/plan selection results. `online.js` retains shadow comparison, feature flags, queue/store mutation, replay execution, public diagnostic getters, and a module-unavailable legacy fallback; wire/storage/reconnect behavior is unchanged.
- `GamePendingResolutionPolicy.hasRenovationTarget()` now owns the shared built-landmark target query used both when Renovation Company activates and when consecutive pending activations are skipped. `GameManager` retains landmark identity adaptation, pending mutation, logs, and phase authority.
- `CPUEvaluation.lookaheadTerminalHeuristic()` now owns expert lookahead terminal score composition, including race-focus and per-opponent threat deductions. `CPU.js` remains the feature adapter; flag/fact order, opponent traversal, all coefficients, fixed decisions, and the full 2–10-player self-play baseline are unchanged.
- Scoped gates remain 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice, save/localStorage shape, Socket.IO protocol, reconnect authority/timing, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 49 architecture boundaries

- `OnlineReconnectState.createCompletionController()` now owns the legacy reconnect-completed projection. `online.js` retains event authority, timers, reconnect effects, transition ordering, and the existing fallback behavior.
- `GameTurnPolicy.hasActiveCardEffect()` now owns active-effect lookup for the post-Airport IT Startup check. `GameManager` retains staged `nextTurn()` orchestration, award/log ordering, dormancy adaptation, and phase authority.
- `CPUEvaluation.expertChoiceScore()` now owns expert position-score and optional lookahead-score composition. `CPU.js` retains profiling, simulation, feature adaptation, and tuning ownership; fixed decisions and the complete 2–10-player self-play baseline are unchanged.
- Scoped gates remain 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice, save/localStorage shape, Socket.IO protocol, reconnect timing/effects, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 50 architecture boundaries

- `OnlineRetryPolicy.createRejoinAttemptController()` now exclusively owns reconnect attempt count and exhausted state; duplicate compatibility variables were removed. `online.js` retains timeout, callback, Socket emit, status, and retry scheduling authority.
- `GameCardActivationPolicy.loanRepaymentPlan()` now owns the 5/6 activation gate, loan-count validation, and coin-capped repayment amount. `GameManager` retains active-card/dormancy traversal, coin mutation, and the exact log.
- `CPUEvaluation.crowdLeaderBonus()` and `crowdCleaningBonus()` now own multiplayer threat-ratio aggregation. `CPU.js` remains the player/card/threat adapter; two-pass traversal, all weights, fixed decisions, and the complete 2–10-player self-play baseline are unchanged.
- Scoped gates remain 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice, save/localStorage shape, Socket.IO protocol, reconnect timing/effects, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 51 architecture boundaries

- `OnlineRestoreLifecycleState.createController()` now exclusively owns restore generation, in-progress, quarantine, and flush state; four parallel compatibility variables were removed. `online.js` retains queue effects, Socket callbacks, replay/abort ordering, and reconnect authority.
- `CPUEvaluation.strongChoiceScore()` now owns strong position-score coefficient composition. `CPU.js` retains feature acquisition, profiling labels/order, cache access, and tuning; fixed decisions and the complete 2–10-player self-play baseline are unchanged.
- `UiPendingEffects.createUpdateController()` now exclusively owns pending-modal update reentrancy state. `ui.js` retains blocking-modal policy, violation reporting, HTML assignment, and DOM interaction order.
- Scoped gates remain 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice, save/localStorage shape, Socket.IO protocol, reconnect timing/effects, modal presentation, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 52 architecture boundaries

- `OnlineRetryPolicy.createActionFlightController()` is now the sole production owner of online action in-flight state and start time. `online.js` retains ACK admission, timeout/retry scheduling, Socket transport, and public state projection; partial test runtimes retain a read-only legacy fallback.
- `CPUEvaluation.purchasePlanValue()` now owns purchase-plan card/landmark/zero score selection. `CPU.js` retains candidate generation, ranking, cache, profiling, and all feature acquisition; the exact urgency and surplus coefficients, fixed decisions, and 2–10-player self-play baseline are unchanged.
- `UiModalPolicy.createRuntimeController()` now owns active modal ID, focus restoration target, and inert restoration entries. `ui.js` retains modal policy, DOM/focus/inert effects, public APIs, and the exact open/close effect order.
- Main CPU scheduler state extraction was deferred because token invalidation is shared across `main.js`, `online.js`, `storage.js`, and `appShell.js`; moving it safely requires a separately guarded effect-order migration.
- Scoped gates remain 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice, save/localStorage shape, Socket.IO protocol, ACK/reconnect timing, modal behavior, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 53 architecture boundaries

- `LocalGameStart.createPendingController()` now exclusively owns local-game RL preload/start pending state. `main.js` retains readiness rendering, Promise effects, game initialization, and lifecycle notification; duplicate-click admission and success/failure ordering are unchanged.
- `AutoSkipPolicy.createScheduleController()` now exclusively owns auto-skip pending state and timer handle. `main.js` retains build availability facts, the exact 1500 ms timer, human-action revalidation, online dispatch, and cancellation effects.
- `DelayedHumanActionPolicy.createScheduleController()` now exclusively owns delayed roll/select pending state, generation token, immutable action snapshot, and timer handle. `main.js` retains visibility effects, action admission, random dice generation, online dispatch, and the exact 600 ms/default deadline behavior.
- Main CPU scheduler state remains deferred because token invalidation crosses `main.js`, `online.js`, `storage.js`, and `appShell.js`; this batch removes independent state without altering that shared effect boundary.
- Scoped gates remain 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice, RNG point, save/localStorage shape, Socket.IO protocol, reconnect timing, UI timing, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 54 architecture boundaries

- `LocalResumePreloadState` is now the sole owner of local-resume pending state; the duplicate `storage.js` projection was removed. `storage.js` retains repository reads, RL preload Promise effects, button rendering, and generation-gated continuation.
- `UiEventDelegation.createBindingController()` now owns static and delegated UI-handler binding state. `main.js` retains listener registration and marks each binding only after the existing registrations complete, preserving retry behavior after missing DOM APIs.
- `OnlineLobbyRequestState` is now the sole mutable owner of create/join pending state; duplicate `online.js` booleans were removed. Existing `onlineCreateRoomPending` / `onlineJoinRoomPending` browser names remain read-only controller projections for PWA update deferral compatibility.
- Scoped gates remain 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice, save/localStorage shape, Socket.IO protocol, lobby timeout/callback order, PWA update deferral, or production rollout default changed.

## 2026-08-04 Batch 55 architecture boundaries

- `GameCardActivationPolicy` now owns frozen player-ordered collection requests for Stadium, Publisher, Tax Office, and IT Startup. `GameManager` retains live card/player adaptation, coin transaction application, pending effects, and exact logs.
- `CPUEvaluation.v2SimpleTvTargetScore()` now owns the complete strongest-CPU TV target base/landmark-denial score. `CPU.js` retains target traversal, feature adaptation, first-max tie order, profiling, and tuning; fixed decisions and the 2–10-player self-play baseline are unchanged.
- `OnlineSocketRegistry.createUnavailableReportController()` now owns the one-shot Socket.IO-unavailable diagnostic state. `online.js` retains user notice/status effects and reports checkpoint/client error only on the first claim, preserving effect order and payloads.
- Scoped gates remain 214 ESLint maintenance files and 213 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice, save/localStorage shape, Socket.IO event/payload, diagnostic payload/order, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 56 architecture boundaries

- `OnlineDiagnosticState.createController()` now owns the 37 plan/effect/shadow diagnostic selections that were writable top-level variables in `online.js`. Existing public diagnostic getters, assignments, Socket event order, authority flags, and legacy fallbacks are unchanged.
- `UiCardSelect.createBindingController()` now owns card-selection modal handler admission. `ui.js` retains DOM lookup/listener registration and deliberately preserves the existing one-shot claim even when the modal is absent.
- `GameCardActivationPolicy.isActivationCandidate()` now owns the shared revived-card, dormant-card, color, and dice admission sequence for red/blue/green/purple processing. `GameManager` retains player adaptation, activation order, all effects, coin mutation, pending state, and exact logs.
- Scoped gates are now 215 ESLint maintenance files and 214 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice, save/localStorage shape, Socket.IO event/payload, reconnect authority/timing, UI presentation, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 57 architecture boundaries

- `OnlineRetryPolicy.createRejoinTimerController()` is now the sole owner of rejoin timer handle and deadline in both legacy and event-authority modes. `online.js` retains authority selection labels, timeout decisions, Socket emits, hostless fallback, status effects, and the existing default-OFF rollout flags.
- `PageActivationPolicy.createLifecycleController()` now owns page-activation handler binding admission and hidden-start time. `main.js` retains listener effects and the exact RL-load, delayed-human-action, online-reconnect, CPU-resume, and checkpoint order.
- `GameEngineClientShadow.createOutcomeController()` now owns the last local shadow/authority diagnostic result. `main.js` retains snapshot adaptation, mutable execution, parity comparison, optional adoption, rendering, and CPU scheduling.
- Scoped gates remain 215 ESLint maintenance files and 214 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice, RNG point, save/localStorage shape, Socket.IO event/payload, reconnect timing/status, Engine authority default, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 58 architecture boundaries

- `OnlineRestoreQueueState.createStore()` is now the sole mutable owner of queued restore events. `online.js` retains three read/replace/append adapter functions, transition/effect authority flags, diagnostic source labels, replay execution, Socket callbacks, and the exact overflow/carry/drain/failure order; the duplicate raw queue and its dual-write branches were removed.
- `CPULegalMoves.expertBuildOptions()` and `strongBuildOptions()` now own ordered build-candidate assembly after affordability and ranking. `CPU.js` retains every score, ranking input, tuning flag, landmark/attack fact, and RNG point; skip/landmark/card order, candidate limits, early multiplayer gates, and fallback behavior are unchanged.
- UI card/landmark selection ownership was inspected but deferred because the compatibility globals are written across `ui.js`, `main.js`, `online.js`, and `storage.js`; migrating one writer would create another shadow state. The cross-runtime CPU scheduler token remains deferred for the same effect-order reason.
- Scoped gates remain 215 ESLint maintenance files and 214 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice/RNG, save/localStorage shape, Socket.IO event/payload, reconnect timing/effect order, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 59 architecture boundaries

- `CpuSchedulerState.createController()` is now the sole mutable owner of CPU schedule token, pending token, and lease deadline. `main.js` retains timeout/CPU-step effects; `online.js` and `storage.js` invalidate through the shared adapter, and `appShell.js` reads a snapshot for diagnostics. Token increments, cancel semantics, callback admission, and scheduler timing are unchanged.
- The existing turn-state, turn-announcer timer, and build-filter controllers are now eagerly constructed in `ui.js`. Their constructors are side-effect free; reset timing, timers, DOM effects, and browser-global APIs are unchanged.
- Card and landmark selection replacement now passes through `replaceEnabledCardSelection()` and `replaceEnabledLandmarkSelection()`, keeping the compatibility `Set` projections and `UiCardSelect` controller synchronized across local start, online start/rejoin, restore, and modal application. A static contract rejects new direct replacement writers in `main.js`, `online.js`, and `storage.js`.
- Full removal of the card/landmark compatibility `Set` projections remains deferred until reads can move behind a neutral runtime boundary without changing script order. Scoped gates remain 215 ESLint maintenance files and 214 checkJs runtime files; whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice/RNG, save/localStorage shape, Socket.IO event/payload, reconnect/CPU scheduling timing, UI presentation, PWA/SW behavior, or production rollout default changed.

## 2026-08-04 Batch 60 architecture boundaries

- `GameSelectionState.runtime` is now the sole mutable owner of enabled card and landmark selection. The ambient `enabledCards` / `enabledLandmarks` compatibility globals were removed; local start, online create/start/rejoin, save/restore, tutorial/build/player views, and the card-select modal consume detached snapshots or sets through the neutral boundary. The modal controller remains a draft only, and production/PWA load order is contract-tested.
- `CPUEvaluation.shouldHoldForLandmark()` now owns landmark-saving candidate traversal, urgency/shortfall tie order, and the final threshold. `CPU.js` only adapts live game/player facts; lazy eligibility reads, every numeric threshold, fixed decisions, and the CPU test suite are unchanged.
- `GameDicePolicy.rerollResetState()` now owns the immutable reroll-start transition for used-reroll, dice reset, empty log, and roll phase. `GameManager` retains admission, RNG, mutable application, reroll execution, and exact result-log order.
- Scoped gates now cover 216 ESLint maintenance files and 215 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice/RNG, save/localStorage shape, Socket.IO event/payload, reconnect timing, dice outcome/log order, UI presentation, or PWA/SW behavior changed.

## 2026-08-04 Batch 61 architecture boundaries

- `OnlineRuntimeState.runtime` is now the sole mutable owner of the ten cross-script online session fields: Socket reference, online/host/replay/reconnect flags, player indices/name, room ID, and reconnect token. Existing identifiers are non-enumerable getter/setter projections, so main/storage/UI/app-shell behavior and assignment order remain unchanged while raw declarations are removed from `online.js`.
- `OnlineSetupState.createController()` now owns online room-creation player count, player settings, and CPU speed. `online.js` retains DOM rendering, RL preload, Socket effects, and payload construction, but reads detached snapshots and updates state only through the controller.
- `GameSetupState.runtime` now owns selected local player count, shared player settings, and gameplay CPU speed across main, restore, and online start/rejoin. Existing identifiers remain compatibility projections with the same in-place settings-array behavior; three writable declarations were removed from `main.js`.
- The next online-state step is to replace compatibility-field assignment groups with named controller transitions and reconnect events. Removing the projections before all cross-script consumers migrate remains deferred. Scoped gates now cover 219 ESLint maintenance files and 218 checkJs runtime files; whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice/RNG, save/localStorage shape, Socket.IO event/payload, reconnect/Socket callback order, room setup behavior, UI presentation, or PWA/SW behavior changed.

## 2026-08-04 Batch 62 architecture boundaries

- `OnlineRuntimeState.runtime` now exposes named session transitions for Socket, online/host/replay/reconnect flags, accepted-room identity, restored identity, player-order projection, and reset fields. Production `online.js`, `storage.js`, and `main.js` writes use these operations; compatibility property setters remain only as a temporary cross-script surface.
- `GameRuntimeState.runtime` is now the sole mutable owner of the live `game`, `cpuPlayers`, `prevCoins`, and `undoState` references. Its snapshot is a frozen reference envelope, not a second Game Engine state or a detached canonical snapshot; GameManager, CPU objects, hydration, and UI effects keep their existing authority and object identity.
- Tutorial enabled/level values now belong to `UiTutorialSettings.runtime`. Main initialization, stored-setting restore, and UI setting changes use the controller while the existing localStorage keys, string values, normalization, and state → persist → controls → render order remain unchanged.
- The next safe state step is to replace remaining live-game compatibility assignments with named install/reset/undo operations and then audit whether compatibility setters can become read-only. This must not merge Game Engine authority, hydration, CPU scheduling, Undo effects, or render effects into the state container.
- Scoped gates now cover 220 ESLint maintenance files and 219 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice/RNG, save/localStorage shape, Socket.IO event/payload, reconnect or Socket callback order, Game Engine authority, UI presentation, or PWA/SW behavior changed.

## 2026-08-04 Batch 63 architecture boundaries

- All production writes to live `game`, `cpuPlayers`, `prevCoins`, and `undoState` now use named `GameRuntimeState.runtime` operations. Compatibility properties remain readable/writable only for classic-script and isolated-runtime compatibility; a static contract rejects new bare writes in main, online, storage, and UI.
- The `nextTurn()` path now has an explicit Airport transition plan between admission and IT continuation. `GameTurnPolicy` owns the immutable award/coin delta, while `GameManager` preserves current-player lookup, mutable coin application, exact log text, IT-card inspection, and turn advancement order.
- Winner streak and prior-winner state now belong to `UiWinner.streakRuntime`. `ui.js` keeps first-presentation admission and persists the same `winStreak` then `lastWinnerName` keys before the existing winner effect pipeline.
- The next live-runtime step is to audit read consumers and make compatibility projections read-only only where classic inline/PWA and isolated runtime contracts permit. The state container must not become a duplicate Game Engine, Snapshot, CPU scheduler, or persistence authority.
- Scoped gates remain 220 ESLint maintenance files and 219 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, Airport amount/log, CPU heuristic/choice/RNG, save/localStorage shape, Socket.IO protocol, reconnect ordering, winner presentation, or PWA/SW behavior changed.

## 2026-08-04 Batch 64 architecture boundaries

- `GameSetupState.runtime` now exposes named operations for player count, CPU speed, complete player-setting replacement, individual setting/name updates, and atomic setup replacement. Production writes in `main.js`, `online.js`, and `storage.js` use these operations at the same positions; the settings array copy and in-place entry/name semantics are unchanged.
- Actual browser globals projected by `GameRuntimeState.runtime` are now getter-only. The generic public `write()` escape hatch was removed, while named game/CPU/coin/Undo operations remain the only production mutation surface. Isolated Node/VM compatibility binding remains writable so existing harnesses can stage fixtures without changing runtime behavior.
- Actual browser globals projected by `OnlineRuntimeState.runtime` are now getter-only. The generic public `write()` escape hatch was removed, while Socket, online/host/replay/reconnect, room identity, and reset transitions remain the only production mutation surface. Socket callbacks, storage adapters, and compatibility reads retain their existing order.
- `GameSetupState` compatibility reads, including the intentionally live settings array, remain until all readers can move to detached snapshots without changing UI editing behavior. Winner-streak compatibility setters are also a separate future audit.
- Scoped gates remain 220 ESLint maintenance files and 219 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice/RNG, save/localStorage shape, Socket.IO event/payload, reconnect/Socket callback order, UI presentation, or PWA/SW behavior changed.

## 2026-08-04 Batch 65 architecture boundaries

- Actual browser globals projected by `UiWinner.streakRuntime` are now getter-only. Winner recording and replacement remain named operations; isolated Node/VM fixtures retain writable bindings. Persistence keys, write order, and winner presentation are unchanged.
- `RLModelCatalog` is now the frozen shared source for distributed RL model metadata and allowed server model IDs. `RLModelPortfolio` and `server.js` consume the same definitions; model IDs, paths, player ranges, registry adoption, and runtime selection are unchanged.
- `CPUEvaluation.strongLandmarkUrgencyFeatures()` now owns the pure board-to-feature projection used by strong landmark urgency scoring. `CPU.js` retains live dependency adaptation, and the existing score function, coefficients, branch order, and RNG behavior are unchanged.
- Scoped gates now cover 221 ESLint maintenance files and 220 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice/RNG, RL model or adoption policy, save/localStorage shape, Socket.IO event/payload, reconnect ordering, winner/UI presentation, or PWA/SW behavior changed.

## 2026-08-04 Batch 66 architecture boundaries

- Actual browser property setters projected by `GameSetupState.runtime` and `UiTutorialSettings.runtime` are now absent. Production updates use named operations; isolated Node/VM fixtures retain writable binding compatibility. The `playerSettings` getter intentionally remains the same live array until its editing consumers move to detached operations.
- `UiDomSnapshot.createRuntime()` now owns app-shell DOM observation: blocking-ancestor checks, interactive-child usability/counting, action-HTML fallback inspection, detached element snapshots, and visibility projection. `appShell.js` retains Action registry adaptation, watchdog classification/recovery, DOM mutation, focus/inert handling, reporting, lifecycle, and PWA effects.
- `CPUEvaluation.strongLandmarkThresholdFeatures()` now owns the frozen board projection for conditional-red landmark threshold pressure. Opponent/card traversal, dormancy reads, current progress reads, remaining-landmark lookup, penalty coefficients, and RNG behavior retain their exact order.
- Scoped gates now cover 222 ESLint maintenance files and 221 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.

No game rule, CPU heuristic/choice/RNG, save/localStorage key or value shape, Socket.IO event/payload, reconnect timing, DOM/focus/recovery effect, UI presentation, or PWA/SW behavior changed.


## 2026-08-04 Batch 67 state and effect boundaries

- `GameSetupState` now clones player-setting objects on ingress and exposes frozen detached arrays/entries on `read()` and `snapshot()`. Production mutation remains exclusively in the existing named operations; selected count, CPU speed, localStorage keys/values, rendering, and RL preload order are unchanged.
- `js/uiRecoveryEffects.js` now owns dependency-injected modal-lock, shell visibility, display, pointer-event, and body-class DOM mutations. `appShell.js` retains recovery admission, modal/focus coordination, checkpoints, watchdog classification, and effect ordering.
- `CPUEvaluation.expertV2SimpleCleaningFeatures()` owns the existing player/card traversal and frozen numeric projection; `expertV2SimpleCleaningScore()` owns the unchanged `opponentValue - selfValue * 1.2` formula. `CPU.js` remains the live-game adapter and preserves dormancy/value callback order.
- Scoped gates now cover 223 ESLint maintenance files and 222 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.
- Rules, CPU strength/RNG, schema/protocol, save/storage formats, reconnect behavior, watchdog recovery decisions, focus/inert ordering, UI presentation, and PWA defaults are unchanged.


## 2026-08-04 Batch 68 explicit read boundaries

- `main.js`, `storage.js`, `ui.js`, and `appShell.js` now read shared player count/settings/CPU speed through `GameSetupState.runtime.snapshot()`. Compatibility properties remain published and read-only for external/classic-script compatibility, but are no longer production consumer dependencies.
- Tutorial rendering, controls, toggles, level cycling, and settings serialization now read through `UiTutorialSettings.runtime.snapshot()`. Stored-setting restoration writes through `runtime.replace()` without the obsolete direct-global fallback; the existing keys, values, normalization, and effect order are unchanged.
- `CPUEvaluation.strongTempoValueFeatures()` now owns the frozen card/board projection for strong dice-tempo scoring. `CPU.js` retains short-circuit admission and delegates the unchanged numeric policy after the same card/player reads.
- Scoped gates remain 223 ESLint maintenance files and 222 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.
- Rules, CPU strength/RNG, schema/protocol, persistence formats, reconnect/Socket ordering, tutorial behavior, UI presentation, and PWA defaults are unchanged.

## 2026-08-05 Batch 69 runtime read and CPU evaluation boundaries

- `appShell.js` now reads live game, CPU, Undo, and online-session state exclusively through `GameRuntimeState.runtime.snapshot()` and `OnlineRuntimeState.runtime.snapshot()`. Client-error context, watchdog snapshots/recovery admission, and lifecycle notification metadata retain the same values and effect order; production and isolated-runtime load order is contract-tested.
- `storage.js` now captures explicit game/online runtime snapshots for local-save admission/serialization and Undo admission/dispatch. Resume hydration obtains the newly installed game from the named `setGame()` result instead of the compatibility projection. Existing keys, serialized shapes, winner short-circuit, hydrate order, and online Undo payload are unchanged.
- `CPUEvaluation.moverValueFeatures()` now owns the ordered, frozen Moving Company candidate projection and `moverValue()` owns the existing formula. `CPU.js` remains the live-game adapter; card/target traversal, callback order, coefficients, tie behavior, fixed decisions, self-play results, and RNG points are unchanged.
- Scoped gates remain 223 ESLint maintenance files and 222 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.
- Rules, CPU strength/RNG, schema/protocol, persistence keys/formats, reconnect/Socket ordering, watchdog/lifecycle behavior, UI presentation, and PWA defaults are unchanged.

## 2026-08-05 Batch 70 UI and main runtime read boundaries

- `ui.js` now reads game/CPU/Undo and online-session state through `GameRuntimeState.runtime.snapshot()` and `OnlineRuntimeState.runtime.snapshot()` across rendering, input admission, pending/build/Undo views, tutorials, and diagnostics. DOM writes, modal behavior, presentation, and event order remain in their existing owners.
- `main.js` now reads online-session state through `OnlineRuntimeState.runtime.snapshot()` for action dispatch, CPU scheduling, reconnect gates, human-action admission, delayed dice actions, builds, and auto-skip. Delayed CPU work takes a fresh snapshot at execution time so it does not retain stale connection/host state.
- Static contracts prevent the removed ambient reads from returning and enforce runtime module load order. Rules, CPU strength/RNG, schema/protocol, persistence formats, reconnect timing, UI presentation, and PWA defaults are unchanged.

## 2026-08-05 Batch 71 complete Game runtime read boundary

- `online.js` now reads game, CPU, and Undo state through `GameRuntimeState.runtime.snapshot()` for host notifications, action-log compaction, resend admission, Snapshot/Undo serialization, replay admission, online game initialization, Engine adaptation/application, and restore hydration. Session storage keys, action payloads, replay order, and Socket effects are unchanged.
- `main.js` now uses the same boundary for local initialization, Engine shadowing, allowed-action checks, CPU strategy/scheduling, human and delayed dice actions, pending resolution, builds, turn completion, and auto-skip. Asynchronous callbacks refresh the envelope at execution time while synchronous decisions retain one consistent envelope.
- `appShell.js`, `storage.js`, `ui.js`, `online.js`, and `main.js` no longer depend on ambient live-game reads in production. Static contracts guard the boundary. Rules, CPU decisions/RNG, persistence formats, protocol, reconnect behavior, UI presentation, and PWA defaults are unchanged.

## 2026-08-05 Batch 72 complete online session read boundary

- `online.js` now reads Socket, online/host/replay/reconnect flags, room/player identity, and reconnect token through operation-scoped `OnlineRuntimeState.runtime.snapshot()` envelopes. The migration covers reconnect observation and retry, room-scoped storage, pending ownership/resend, session persistence, Socket connect/disconnect, host migration, restore/replay callbacks, hostless restore, create/join, action sending, and recreate payloads.
- Delayed and Socket callbacks refresh the session envelope at execution time; synchronous plans use one envelope where consistency matters. Named runtime transitions remain the only production writers. Socket.IO event names/payloads, ACK timing, restore ordering, storage keys/formats, authority flags, and fallback paths are unchanged.
- `online.js` passes its behavior and online E2E suites, but whole-file ESLint/checkJs inclusion remains deferred: direct lint activation exposed 373 classic-script dependency names. Adding ambient declarations would hide rather than remove that coupling, so typed/lint expansion follows further dependency injection.

## 2026-08-05 Batch 73 client effect and lifecycle boundaries

- `js/onlineClientEffects.js` is now the explicit adapter for online rendering, CPU scheduling/invalidation, notices, resume-button refresh, UI-lock reset, and lifecycle-start notification. `online.js` resolves late-loaded classic-script effects through one named map; required/optional behavior and call forwarding are contract-tested. Socket.IO events/payloads, ACK/retry timing, restore order, storage keys/formats, and authority flags are unchanged.
- `js/lifecycleRuntime.js` now owns lifecycle notification state, runtime fact collection, session/dedupe transitions, payload construction, storage marker handling, and transport admission. `appShell.js` retains only dependency wiring and its existing public compatibility functions. Current and legacy opt-out keys, payload fields, start suppression, checkpoints, and start/finish ordering are fixed by unit and integration tests.
- Both new modules are covered by scoped ESLint and checkJs. Scoped gates now cover 225 ESLint maintenance files and 224 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`; whole-file activation for `online.js` still follows further dependency-boundary work rather than ambient-global declarations.
- Rules, CPU strength/RNG, schema/protocol, persistence formats, reconnect behavior, notification semantics, UI presentation, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 74 watchdog and online DOM effect boundaries

- `js/uiRecoveryEffects.js` now owns the repeated watchdog DOM mutations for querying action children, inserting a missing Undo action, and releasing disabled/hidden/inert/ARIA/display/pointer locks with explicit options. `appShell.js` retains freeze classification, recovery admission, action-container selection, render/checkpoint order, CPU rescheduling, online resync, and reporting. Existing Safari CPU-stall, ACK-stall, pending/modal, post-build Undo, and interaction-lock integration contracts remain green.
- `js/onlineDomEffects.js` now owns online status text/HTML, waiting-state reads, lobby button views, input values, player-setting projection, and title/game-screen switching. `online.js` no longer calls `document.getElementById`; Socket callbacks and reconnect/restore/lobby orchestration retain their existing order and use the injected DOM runtime.
- Production, PWA cache, isolated online runtime, integration runtime, load-order, ESLint, and checkJs lists include the new boundary. Scoped gates now cover 226 ESLint maintenance files and 225 checkJs runtime files. Whole-file exclusions remain `appShell.js`, `main.js`, `online.js`, `storage.js`, and `ui.js`.
- Rules, CPU strength/RNG, schema/protocol, storage formats/keys, reconnect authority/timing, watchdog recovery decisions, notification behavior, UI text/HTML, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 75 transport and app-shell runtime effect boundaries

- `js/onlineSocketEffects.js` is now the sole outgoing Socket emit boundary for `online.js`. The five fixed room/action events and the three injected hostless-restore events retain their exact names, payload identity, override-socket behavior, and fail-closed handling; a source contract prevents direct `.emit()` calls from returning to the orchestrator.
- `js/appShellRuntimeEffects.js` now owns late resolution and invocation of rendering, build-menu refresh, CPU scheduler health/schedule/cancel, online action timeout recovery, resume, RL preload, settings, skyline, and resume-button effects. `appShell.js` retains watchdog/crash/PWA policy and effect order while no longer invoking those ambient runtime functions directly.
- Production/PWA assets, isolated runtimes, release runtime, load order, ESLint, and checkJs include both boundaries. Scoped gates now cover 228 ESLint maintenance files and 227 checkJs runtime files; the five orchestration files remain whole-file exclusions.
- Rules, CPU strength/RNG and scheduling policy, Socket.IO names/payloads/ACK timing, restore/reconnect order, storage formats/keys, UI output, lifecycle semantics, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 76 app-shell recovery and event ownership

- `js/uiWatchdogAsyncRecovery.js` now owns CPU-turn-stall and online-action-flight-stall recovery admission, scheduler/timeout invocation, recovered-state observation, and checkpoint payload/order through injected effects. `appShell.js` retains freeze classification and handler selection; Safari CPU-stall and online ACK-stall integration contracts remain unchanged.
- `ClientEventRuntime.createShellBindings()` now owns one-time crash/rejection/console, online/offline, resize, watchdog interval, and PWA-install handler registration. `appShell.js` retains public compatibility wrappers and startup order without directly managing binding keys or replacing console callbacks.
- `appShell.js` decreased from 1,391 to 1,346 lines in this batch. Scoped gates now cover 229 ESLint maintenance files and 228 checkJs runtime files.
- Game rules, CPU decisions/RNG and scheduler policy, online timeout/reconnect behavior, report payloads, browser event order, storage, UI output, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 77 app-shell observation runtime

- `js/appShellObservationRuntime.js` now owns watchdog DOM observation, buildable-card/landmark candidate detection, action-child/container usability, registry-drift observations, interactability issue assembly, primary-button state, and complete client runtime snapshot construction through injected game/online/DOM/runtime dependencies.
- `appShell.js` retains browser dependency wiring, compatibility entry points, modal/focus recovery coordination, freeze classification, reporting, lifecycle, PWA, and startup ordering. Its size decreased from 1,346 to 1,069 lines; the 426-line observation owner is independently linted, type-checked, and unit-tested.
- Existing integration coverage fixes post-build, pending, modal, Safari CPU-stall, online ACK-stall, selector drift, missing Undo, card-filter, winner, reconnect, and checkpoint behavior across the new boundary. Scoped gates now cover 230 ESLint maintenance files and 229 checkJs runtime files.
- Rules, CPU decisions/RNG, scheduler and watchdog policy, Action/Socket/schema contracts, persistence, UI recovery/output, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 78 app-shell UI recovery runtime

- `js/uiWatchdogRecoveryRuntime.js` now owns watchdog render synchronization, action-container/child repair, post-build Undo restoration, pending/human/modal/stale-modal recovery, freeze-handler selection, and before/after recovery checkpoint orchestration through injected policy, observation, and effect boundaries.
- `appShell.js` retains browser composition, its established compatibility entry points, modal/focus primitives, reporting, lifecycle, PWA, and startup ordering. Its size decreased from 1,069 to 855 lines; the 340-line recovery runtime is independently linted, type-checked, unit-tested, and covered by the existing integration suite.
- Production/PWA assets and isolated main/integration/release runtimes load the new boundary before `appShell.js`. Scoped gates now cover 231 ESLint maintenance files and 230 checkJs runtime files.
- Recovery eligibility, render/checkpoint/effect order, CPU scheduling, online ACK timeout behavior, rules, CPU strength/RNG, protocol, persistence, UI output, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 79 watchdog execution runtime

- `js/uiWatchdogRuntime.js` now owns freeze classification inputs, ACK-timeout policy lookup, progress/dedupe decisions, diagnostic compaction, recovery dispatch, freeze-snapshot persistence, and report execution through injected observation, monitor, recovery, storage, and reporting boundaries.
- `OnlineRetryPolicy` remains late-resolved so production and isolated online runtimes preserve their classic-script load behavior. `appShell.js` retains dependency composition plus the established `classifyLikelyFreeze` and watchdog callback entry points; its size decreased from 855 to 775 lines.
- Focused contracts fix report/recovery/storage ordering, duplicate-report recovery, timeout resolution, and fail-fast dependencies. Main and integration coverage preserve Safari CPU stall, online ACK stall, pending/modal locks, and report payload behavior. Scoped gates now cover 232 ESLint maintenance files and 231 checkJs runtime files.
- Rules, CPU decisions/RNG, scheduler thresholds, online ACK/reconnect behavior, protocol, persistence formats, report schema, UI recovery, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 80 app-shell UI lock runtime

- `js/appShellUiLockRuntime.js` now owns modal-state projection, stale confirm/pending cleanup, shell/game-screen lock clearing, game-reset lock cleanup, post-build next-turn stabilization, and human-turn unlock/render synchronization through injected watchdog policy and DOM/runtime effects.
- `appShell.js` retains classic-script dependency resolution and compatibility entry points used by `main.js`, `ui.js`, `online.js`, and `storage.js`. Its size decreased from 775 to 650 lines; the 280-line UI lock owner is independently linted, type-checked, and unit-tested.
- Focused tests fix stale-confirm cleanup, the unchanged `[0, 250, 1500, 3500]` post-build schedule, and human-turn render/action-lock ordering. Existing integration coverage preserves pending/modal/accessibility recovery and online/CPU freeze behavior. Scoped gates now cover 233 ESLint maintenance files and 232 checkJs runtime files.
- UI text, modal/focus semantics, game rules, CPU decisions/RNG, scheduler policy, online protocol/reconnect, persistence, reports, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 81 app-shell client reporting runtime

- `js/appShellClientReportingRuntime.js` now owns client URL/context capture, freeze-stack normalization, report construction/keying, 10-second duplicate admission, transport dispatch, and manual debug-report assembly through injected game/online/browser/checkpoint dependencies.
- `appShell.js` retains composition plus the existing `buildClientErrorReport`, `reportClientError`, and debug window entry points. Its size decreased from 650 to 600 lines; direct transport ownership is guarded against returning to the composition root.
- Focused contracts preserve query/hash redaction, runtime context fields, `/api/client-error`, duplicate suppression, debug checkpoint/snapshot content, and fail-fast dependency validation. Scoped gates now cover 234 ESLint maintenance files and 233 checkJs runtime files.
- Report payload/schema, notification behavior, game rules, CPU decisions/RNG, online protocol/reconnect, persistence, UI, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 82 app-shell crash recovery runtime

- `js/appShellCrashRuntime.js` now owns crash-screen single-show admission, CPU cancellation, view application, focus-trap planning/effects, keydown listener identity, hide, and resume ordering through injected policy/effect/browser dependencies.
- `appShell.js` retains dependency composition and the existing `showCrashScreen` / `crashResume` globals used by startup and delegated UI flows. Its size decreased from 600 to 581 lines.
- Focused contracts preserve CPU-stop-before-view order, duplicate-show suppression, saved-game resume selection, identical focus-handler removal, and remove-listener → hide → resume order. Scoped gates now cover 235 ESLint maintenance files and 234 checkJs runtime files.
- Crash text/focus behavior, saved-game handling, game rules, CPU decisions/RNG, online protocol/reconnect, persistence formats, reporting, UI, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 83 app-shell startup runtime

- `js/appShellStartupRuntime.js` now owns online-availability projection, PWA install-controller delegation, and the ordered main-view startup sequence across settings, RL preload, resume/skyline refresh, browser handlers, and watchdog start.
- `appShell.js` retains classic-script dependency composition and compatibility delegates. Focused contracts fix startup effect order, preload reasons, online projection, and PWA method forwarding. Scoped gates now cover 236 ESLint maintenance files and 235 checkJs runtime files.
- Whole-file checkJs admission for `appShell.js` remains deferred after an explicit audit found 102 ambient classic-script references; these must be removed through real boundaries rather than hidden with broad declarations.
- Startup order, online/offline presentation, PWA prompts, game rules, CPU behavior, protocol, reconnect, and persistence are unchanged.

## 2026-08-05 Batch 84 Engine shadow parity expansion

- The deterministic Engine shadow matrix now covers Stadium income across every opponent when some opponents cannot pay the full two coins.
- The fixture runs for 2, 3, 5, and 10 players across independent action/snapshot schema v0/v1 selections, fixing both multiplayer iteration and partial-payment semantics before any authority rollout.
- This is test-only: game rules, CPU behavior/RNG, save formats, online protocol/reconnect, localStorage, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 85 local game start runtime

- `js/localGameStartRuntime.js` now owns local player-count/type/name transitions, settings rendering, RL model readiness/preload lifecycle, duplicate-start admission, and the ordered local-game start effects.
- `main.js` retains browser-global compatibility delegates and dependency composition; its size decreased from 1,491 to 1,391 lines. A focused runtime contract fixes the non-RL effect order and the RL pending/frozen-settings behavior.
- The new boundary is included in browser script-order, Service Worker asset, integration-loader, ESLint, and checkJs guards. Scoped gates now cover 237 ESLint maintenance files and 236 checkJs runtime files.
- Game rules, CPU selection/RNG, settings/save formats, localStorage keys, online protocol/reconnect, and PWA cache strategy are unchanged.

## 2026-08-05 Batch 86 local game initializer

- `js/localGameInitializer.js` now owns local-game reset-before-init ordering, enabled card/landmark stock application, normalized setting projection, player-order shuffle, CPU construction, first log, render, and CPU scheduling.
- `main.js` retains dependency composition and its `init` compatibility delegate; its size decreased from 1,391 to 1,367 lines. The runtime accepts injected randomness, making player/CPU creation order directly reproducible in focused tests.
- Contracts fix reset/effect order, fixed-random player order, names, expert CPU options, stock values, first log, and retain the mutable CPU-array identity installed in `GameRuntimeState`. Scoped gates now cover 238 ESLint maintenance files and 237 checkJs runtime files.
- Game rules, CPU tuning/decisions/RNG consumption count, save formats, online protocol/reconnect, localStorage keys, and PWA behavior are unchanged.

## 2026-08-05 Batch 87 local game restart runtime

- `js/localGameRestartRuntime.js` now owns restart confirmation, saved/local online-session cleanup, scheduler and delayed-action cancellation, online/UI/lifecycle reset, game/setup reset, title-screen projection, redraw, PWA refresh, and diagnostic checkpoint ordering.
- `main.js` retains explicit dependency composition and its `restartGame` compatibility delegate. Focused contracts preserve the five legacy online storage keys, facade-first/fallback cleanup, exact reset reason strings, and the complete confirmed-start → confirmed-complete effect order.
- Existing main and integration tests continue to cover UI lock removal, lifecycle notification re-arming, watchdog silence, and title/game-screen state. Scoped gates now cover 239 ESLint maintenance files and 238 checkJs runtime files.
- Storage keys/formats, game rules, CPU behavior, online protocol/reconnect, modal behavior, and PWA update behavior are unchanged.

## 2026-08-05 Batch 88 page activation runtime

- `js/pageActivationRuntime.js` now owns page visibility/pageshow binding, hidden-duration lifecycle state, delayed human-action token/timer/state, deadline resume decisions, RL/online/CPU resume orchestration, and activation diagnostics.
- `main.js` retains injected browser/game effects and compatibility delegates; its size decreased from 1,369 to 1,319 lines. The two mutable controllers now live together with the use case that coordinates them.
- Focused contracts preserve deadline reschedule/run-once behavior, listener single-binding, hidden-page CPU suppression, stale CPU lease renewal, and RL → delayed human → online → CPU → checkpoint ordering. Scoped gates now cover 240 ESLint maintenance files and 239 checkJs runtime files.
- Dice RNG, CPU decisions/scheduling policy, delayed-action eligibility, online reconnect behavior, game rules, storage, and PWA behavior are unchanged.

## 2026-08-05 Batch 89 main UI event runtime

- `js/mainUiEventRuntime.js` now owns `data-ui-*`/`data-action` event targeting, command dispatch, static and delegated listener registration, keyboard role-button activation, speed-label projection, Business Center button context, and PWA apply/banner fallbacks.
- `main.js` retains a late-resolved effect registry and compatibility handler delegates; its size decreased from 1,319 to 1,174 lines. UI command interpretation and binding state no longer sit beside game-action implementation.
- Focused contracts preserve command arguments, listener single-binding, reload fallback, and the existing four delegated containers; main/integration contracts retain every static, dice, pending, build, player, keyboard, and PWA path. Scoped gates now cover 241 ESLint maintenance files and 240 checkJs runtime files.
- Game actions, selectors/data attributes, modal behavior, CPU/RNG, online protocol, persistence, and PWA update semantics are unchanged.

## 2026-08-05 Batch 90 local Game Engine runtime

- `js/localGameEngineRuntime.js` now owns local human/CPU action routing, online-send branching, mutable fallback application, snapshot serialization, runtime adapter construction, determinism admission, shadow transition, parity finish/outcome state, and authority snapshot adoption.
- `main.js` retains injected game/domain adapters and compatibility delegates; its size decreased from 1,174 to 1,096 lines. Shared Engine rollout mechanics no longer sit inline with CPU scheduling and UI actions.
- Focused and full local-shadow contracts preserve online send-only behavior, checkpoint/render/schedule order, unresolved-random rejection, all deterministic action parity, build/Undo, mutable identity under default-OFF, and late-bound Engine replacement/fallback. Scoped gates now cover 242 ESLint maintenance files and 241 checkJs runtime files.
- Production flags remain absent/default-OFF. Rules, CPU decisions/RNG, Action/Snapshot formats, online protocol, persistence, and PWA behavior are unchanged.

## 2026-08-05 Batch 91 CPU turn scheduler runtime

- `js/cpuTurnSchedulerRuntime.js` now owns CPU schedule token/lease/pending-timer state, transport/game blocking health, phase-step admission, timer chaining, stale cancellation, cooldown, error recovery, and the public scheduler facade.
- `main.js` retains the phase handlers and action-selection/execution functions while supplying runtime effects; its size decreased from 1,096 to 930 lines. Scheduler orchestration no longer surrounds CPU strategy code.
- Focused contracts preserve CPU-speed delay, 500ms cooldown, non-host rejection, cancel/stale-timer behavior, and checkpoint labels. Existing main/integration plus full CPU decision/self-play baselines preserve phase execution, online gates, build-failure pass, fixed decisions, and CPU strength. Scoped gates now cover 243 ESLint maintenance files and 242 checkJs runtime files.
- Phase handler order, CPU choices/RNG, difficulty tuning, rules, online protocol/reconnect, persistence, and PWA behavior are unchanged.

## 2026-08-05 Batch 92 CPU phase handlers

- `js/cpuPhaseHandlers.js` now owns the ordered roll/select/reroll/harbor/pending/build/nextTurn/IT execution adapters between `CpuTurnStrategy` proposals and local/online game effects.
- `main.js` retains proposal construction and injected action/game effects; its size decreased from 930 to 836 lines. Scheduler orchestration, strategy selection, and phase execution adapters are now separate boundaries.
- Focused contracts freeze all eight handler names/order, resolved roll arguments, and local-versus-online build-failure behavior. Existing main/integration and full CPU decision/self-play baselines preserve every phase result, action trace, RNG, and CPU strength. Scoped gates now cover 244 ESLint maintenance files and 243 checkJs runtime files.
- CPU heuristics/tuning, proposal meaning, handler order, rules, online protocol, persistence, and PWA behavior are unchanged.

## 2026-08-05 Batch 93 human Action runtime

- `js/mainHumanActionRuntime.js` now owns local-human authority admission, delayed roll/select execution, canonical dice/pending payload construction, confirmed card/landmark builds, and turn-end sequencing through injected Game, online, PageActivation, Engine-shadow, and UI effects.
- `main.js` retains dependency composition and compatibility delegates; its size decreased from 836 to 674 lines. Human Action execution no longer sits beside local setup, CPU scheduling, UI binding, and rendering helpers.
- Focused contracts preserve delayed RNG and payload order, online ACK blocking, local build shadow/effect order, online send-only builds, Airport skip/Undo order, and ownership. Existing main/integration/online/PWA/release gates preserve stale-action rejection, reconnect authority, UI recovery, and browser load/cache contracts. Scoped gates now cover 245 ESLint maintenance files and 244 checkJs runtime files.
- Game rules, CPU decisions/strength/RNG, Action and Socket.IO payloads, save/localStorage formats, reconnect behavior, confirmation text, and PWA/SW semantics are unchanged.

## 2026-08-05 Batch 94 online inbound Action runtime

- `js/onlineInboundActionRuntime.js` now owns the common `gameAction` / `actionAccepted` pipeline: schema decode, restore-event admission, reconnect-state authority selection, sequence decision, replay apply/recovery, and commit dispatch.
- `online.js` retains Socket.IO binding and concrete schema/state/effect/storage adapters; its size decreased from 3,999 to 3,890 lines. Remote Action and sender ACK semantics are now represented by explicit channel contracts instead of duplicated nested handlers.
- Focused contracts preserve effect order, unrelated-ACK isolation, duplicate ACK cleanup, malformed-wire flight behavior, apply-failure return values, and legacy fallback when event authority is not ready. Full online/integration/PWA/release gates preserve restore queueing, gap recovery, Undo/action-log metadata, reconnect, event order, and cache loading. Scoped gates now cover 246 ESLint maintenance files and 245 checkJs runtime files.
- Socket.IO event names/order and payloads, Action/Snapshot schema, ACK timing, restore/reconnect semantics, storage formats/keys, game rules, CPU behavior, UI messages, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 95 online lobby/start runtime

- `js/onlineLobbyStartRuntime.js` now owns room acceptance/status projection and the schema-checked lobby-to-active-game transaction, including restore-generation initialization, normalized bundle persistence, RL preload gating, game/UI activation, version diagnostics, sequence activation, and queued-event release.
- `online.js` retains ordered Socket.IO registration and concrete schema, state-machine, storage, Game, UI, and inbound-handler adapters; its size decreased from 3,890 to 3,853 lines. The new-game lifecycle no longer lives as a large closure inside `initSocket`.
- Focused contracts preserve room create/join/list effects, exact start ordering, schema rejection, version warnings, stale/failed preload behavior, and storage-failure isolation. Full online/integration/PWA/release gates preserve queued Action/host changes, room-scoped restore data, host correction, reconnect, load order, and cache assets. Scoped gates now cover 247 ESLint maintenance files and 246 checkJs runtime files.
- Socket.IO event names/order/payloads, Action/Snapshot schemas, restore/reconnect semantics, storage formats/keys, game rules, CPU decisions/strength, UI messages, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 96 online rejoin activation runtime

- `js/onlineRejoinActivationRuntime.js` now owns the generation-guarded rejoin completion transaction: persisted rejoin effects, game replay, restore activation, queued-event release, and pending outbound Action clear/resend.
- `online.js` retains the `rejoinData` decode/admission preparation and concrete Game, reconnect, storage, Socket.IO, and diagnostics adapters; its size decreased from 3,853 to 3,696 lines. Existing replay/activation/resend pure plans, executor flags, and legacy fallbacks remain independently selectable.
- Focused contracts preserve executor and legacy effect order, stale-generation rejection, replay-failure cleanup/abort, queue-flush short circuiting, and pending clear/resend identity. Full unit/online integration/PWA/release gates include the 4-player completion E2E with compression, host migration, and reconnect. Scoped gates now cover 248 ESLint maintenance files and 247 checkJs runtime files.
- Socket.IO event names/order/payloads, Action/Snapshot schemas, restore/reconnect and ACK semantics, localStorage keys/formats, game rules, CPU decisions/strength/RNG, UI messages, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 97 online rejoin preparation runtime

- `js/onlineRejoinPreparationRuntime.js` now owns the pre-activation rejoin transaction: restore-generation/queue carry start, local original-host bundle ranking, host metadata normalization, pending outbound reconciliation, runtime persistence planning, and best-effort restore-bundle persistence.
- `online.js` retains wire decode/schema admission and concrete state, storage, Socket.IO, Game, and diagnostics adapters; its size decreased from 3,696 to 3,539 lines. The `rejoinData` handler now composes preparation and activation runtimes instead of defining ranking, reconciliation, and persistence closures.
- Focused contracts preserve queue generation carry, newer original-host bundle short circuiting, foreign-session pending removal, accepted-pending cleanup, executor/legacy persistence order, signed/unsigned action-log selection, and storage-failure isolation. Full unit/online integration/PWA/release gates preserve compressed snapshots, Undo/pending replay, host migration, reconnect, and 4-player completion. Scoped gates now cover 249 ESLint maintenance files and 248 checkJs runtime files.
- Socket.IO event names/order/payloads, Action/Snapshot schemas, restore/ACK/reconnect semantics, storage keys/formats, game rules, CPU decisions/strength/RNG, UI text, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 98 online rejoin transaction runtime

- `js/onlineRejoinRuntime.js` now owns the complete top-level `rejoinData` transaction: wire decode, Snapshot/Game schema admission, preparation, RL model preload, generation-safe failure handling, and activation.
- `OnlineRejoinActivationRuntime` is now reusable across rejoin attempts: bundle persistence is supplied per `handle` call instead of being captured by a per-payload runtime closure. `online.js` retains Socket.IO registration and concrete schema, state, storage, Game, and UI adapters; its size decreased from 3,539 to 3,504 lines.
- Focused contracts preserve decode/admission order, local-bundle short circuiting, synchronous/asynchronous preload, stale-generation rejection, exact failure status, prepared-context identity, persistence-before-replay, and browser dependency order. Full unit/online/PWA/release gates pass. Scoped gates now cover 250 ESLint maintenance files and 249 checkJs runtime files.
- Socket.IO event names/order/payloads, Action/Snapshot schemas, restore/ACK/reconnect semantics, storage keys/formats, game rules, CPU decisions/strength/RNG, UI text, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 99 online game initializer

- `js/onlineGameInitializer.js` now owns online-game reset, Game construction, enabled card/landmark stock application, server-supplied player order, CPU creation, local player index projection, first log, render, and CPU scheduling.
- `online.js` retains lazy dependency composition and its `initOnlineGame` compatibility delegate; lazy construction preserves classic-script startup where `SHOP_STOCK` becomes available after `online.js` evaluation. Its size decreased from 3,504 to 3,494 lines.
- Focused contracts preserve exact effect order, shuffled names, CPU opponent settings and RL model ID, missing-local-index fallback, stock/landmark defaults, first log, browser dependency order, and delegation. Full unit/online/release/static gates pass; PWA asset/load tests also pass. Scoped gates now cover 251 ESLint maintenance files and 250 checkJs runtime files.
- Game rules, card stock values, player order, CPU decisions/strength/RNG, Socket.IO schemas/events, save/storage, reconnect, and PWA/SW semantics are unchanged.

## 2026-08-05 Batch 100 online lobby request runtime

- `js/onlineLobbyRequestRuntime.js` now owns create/join input admission, pending generations and timeout effects, RL model readiness/preload, frozen player settings, schema-capability payload construction, and ordered Socket emission.
- `online.js` retains lazy dependency composition and compatibility delegates for existing browser callers; its size decreased from 3,494 to 3,421 lines. The controller, DOM/Socket effects, schema capability provider, setup state, and model portfolio are explicit adapters.
- Focused contracts preserve trimmed/uppercased input, exact payloads, player-name/host/request ordering, duplicate submission exclusion, async RL settings identity, timeout generation, notices, and missing-Socket behavior. Full unit/online/static/PWA/release gates pass. Scoped gates now cover 252 ESLint maintenance files and 251 checkJs runtime files.
- Socket.IO event names/payloads, schema negotiation, hostless capability value, timeout duration/text, CPU model choice, player settings, storage/reconnect, game rules, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 101 online Game Engine runtime

- `js/onlineGameEngineRuntime.js` now owns online replay shadow preparation, detached Engine transition, mutable application orchestration, Undo snapshot lifecycle, parity finish/diagnostics, and guarded authority adoption.
- `online.js` retains lazy adapter composition, mutable Game application, and compatibility delegates used by inbound/rejoin paths; its size decreased from 3,421 to 3,402 lines. Existing test replacement of the adoption delegate remains supported, while the runtime owns the production transaction.
- Focused contracts preserve default-OFF mutable-only execution, build Undo capture, next-turn/Undo clearing before live comparison, detached transition, reconstruction equality, adoption fallback, diagnostics, and absent-shadow fallback. Full unit/online/smoke/static/PWA/release gates pass. Scoped gates now cover 253 ESLint maintenance files and 252 checkJs runtime files.
- Production Engine authority remains default-OFF. Game rules, Action/Snapshot schemas, CPU decisions/RNG, Socket.IO/reconnect, storage, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 102 UI modal DOM effects

- `js/uiModalDomEffects.js` now owns modal focusable filtering/focus, computed visibility projection, open visual normalization, background inert capture/restore, and orphan lock cleanup.
- `ui.js` retains modal policy/application orchestration and compatibility delegates while injecting document/window access, runtime state, trace, and blocking-modal observation; its size decreased from 1,567 to 1,485 lines. Modal policy, open/close plans, and DOM mechanics are now separate layers.
- Focused contracts preserve hidden/disabled/ancestor/computed focus filtering, native and absent-native inert restoration, aria-hidden/pointer restoration, blocking-modal orphan guard, body class cleanup, trace payload, and nested content normalization. Full unit/UI/smoke/static/PWA/release gates pass. Scoped gates now cover 254 ESLint maintenance files and 253 checkJs runtime files.
- Modal IDs/policies, focus order, inert behavior, open/close authority flags, UI layout, game rules, online flow, storage, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 103 UI modal application runtime

- `js/uiModalRuntime.js` now owns blocking admission/violation diagnostics, open and close plan selection, legacy/pure effect execution, active/focus lifecycle, pending/trace sequencing, and Escape/Tab focus-trap commands.
- `ui.js` retains authority-flag reads, concrete state/DOM/trace dependency composition, document keydown binding, and compatibility delegates; its size decreased from 1,485 to 1,368 lines. Modal policy, application transaction, and DOM effects now have explicit separate ownership.
- Focused contracts preserve open focus-before-inert order, close unlock→pending→focus→trace order, legacy/pure-plan parity, nested-blocking denial diagnostics, violation retention inputs, Escape close, Tab wrap, and missing modal behavior. Full unit/UI/smoke/static/PWA/release gates pass. Scoped gates now cover 255 ESLint maintenance files and 254 checkJs runtime files.
- Modal IDs/policies, default-OFF authority flags, focus/inert behavior, UI layout, game rules, online flow, persistence, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 104 CPU roll decision boundary

- `js/cpuRollDecision.js` now owns dice-count, reroll, and Harbor-bonus decisions for weak/normal/strong/expert and expert-v2-simple modes, including the existing profiling scopes and lookahead callbacks.
- `CPU.js` retains its public methods as compatibility delegates plus the scoring, tuning, simulation, and expert helpers supplied to the decision boundary; its size decreased from 3,803 to 3,546 lines. Roll decision orchestration no longer sits inside the main strategy class.
- Focused contracts preserve normal thresholds and evaluation order, weak-mode random consumption, and all three public delegates. The fixed decision snapshot/baseline, full CPU/self-play/simulation, unit, smoke, online, RL, PWA, release, lint, and checkJs gates pass. Scoped gates now cover 256 ESLint maintenance files and 255 checkJs runtime files.
- Difficulty presets, heuristic values, candidate/tie ordering, random-call count, game rules, Action/Snapshot schemas, online protocol/reconnect, persistence, and PWA/SW behavior are unchanged.

## 2026-08-05 Batch 105 CPU pending decision boundary

- `js/cpuPendingDecision.js` now owns TV target, Business Center exchange, Cleaning Company target, Moving Company move, Renovation target, and IT Venture investment decisions across all rule-based difficulties.
- `CPU.js` retains public compatibility delegates, pending execution fallback, and the scoring/tuning/lookahead helpers injected into the decision boundary; its size decreased from 3,546 to 3,148 lines. Pending choice policy is now separate from pending proposal validation/application in `CPUPendingResolution`.
- Focused contracts preserve TV first-win ties, no-candidate short circuiting, expert-v2-simple cleaning/mover candidate order, IT trace order, and all six public delegates. Fixed decision snapshot/baseline, full CPU/self-play/simulation, unit, static, PWA, release, lint, and checkJs gates pass. Scoped gates now cover 257 ESLint maintenance files and 256 checkJs runtime files.
- Pending effect execution, candidate/tie order, heuristic values, random consumption, rules, schemas, online/reconnect, persistence, and PWA/SW behavior are unchanged.
